# MonglePet 테스트 샘플

`ReadOnlySample.monglepet`은 읽기 전용 패키지 가져오기와 편집 가능한 사본 만들기를 검증하기 위한 임시 샘플이다.

설정의 `MonglePet 패키지 가져오기…`에서 이 디렉터리를 선택한다. 가져오기가 끝나면 앱 라이브러리에 독립적으로 복사되므로 테스트 완료 후 `Samples/` 디렉터리 전체를 삭제해도 된다.

이 샘플은 앱에 포함된 `PlaceholderPet` 자산을 재사용하며 배포용 펫이 아니다.
