# 이미지 편집 결과 미리보기와 스크롤 다듬기

## 상태

- 상태: completed
- 생성일: 2026-08-24
- 마지막 갱신: 2026-08-24

## 목표

- 스프라이트 선택 영역과 개별 PNG crop 결과를 실제 저장 프레임처럼 투명 격자와 명확한 경계 안에서 확인한다.
- 확대·축소 컨트롤의 크기를 통일하고 PNG 편집기의 세로 잘림과 스크롤 불가 문제를 해결한다.
- macOS 기준 UI를 확정한 뒤 Windows 편집기 인계 조건을 같은 결과로 갱신한다.

## 범위

- crop·flip 결과 공통 미리보기 캔버스
- 투명 격자, 바깥 캔버스 테두리와 실제 crop 프레임 경계
- 단일 PNG의 상시 결과 미리보기와 다중 PNG 결과 탐색
- 확대·축소 버튼의 동일한 hit area·높이·아이콘 크기
- PNG 편집 본문의 전체 세로 스크롤과 확대 캔버스의 독립 pan
- macOS 테스트·빌드·실제 화면 검토와 Windows 인계 갱신

## 제외 범위

- crop·flip·atlas 픽셀 처리 순서 변경
- `.monglepet` schema 변경
- Windows 소스·빌드·실제 QA
- 앱 버전 확정, Preview ZIP과 GitHub Release 게시

## 열린 질문

- 없음. 모든 추가 수정과 검토가 끝난 뒤 별도 새 릴리스를 게시한다.

## 결정사항

- 결과 미리보기는 새 펫 애니메이션 프레임 미리보기와 같은 투명 격자·캔버스 외곽선 구조를 사용한다.
- 파란 실선은 실제 crop 결과의 전체 프레임 경계를 나타내며 투명 여백도 경계 안에 포함한다.
- 확대 배율이 1×일 때 편집 캔버스가 바깥 페이지 스크롤을 가로채지 않고, 1×보다 클 때만 내부 양축 pan을 제공한다.
- 공개 `1.3.0 (5)`는 그대로 보존하며 다음 버전과 태그는 후속 요청이 모두 끝난 시점에 확정한다.

## 작업 순서

### 공통 계약

- [x] 1단계: 결과 미리보기·경계·스크롤 동작을 결정과 Windows 인계에 기록한다.

### macOS

- [x] 2단계: 공통 crop 결과 캔버스와 단일·다중 PNG 결과 미리보기를 구현한다.
- [x] 3단계: 확대 버튼 정렬과 PNG 전체 스크롤을 수정한다.
- [x] 4단계: 관련 단위 테스트, Debug 빌드와 실제 화면을 검토한다.

### Windows

- [x] 5단계: WinUI 네이티브 구현 결과와 필수 QA를 인계 문서에 추가한다.

### 플랫폼 동등성

- [x] 6단계: Windows 구현·실제 QA 전에는 동등 완료로 표시하지 않는다.

## 검증 방법

- 가로·세로·정사각 crop이 투명 격자 안에 aspect-fit되고 파란 프레임 경계가 잘리지 않는지 geometry 테스트한다.
- 스프라이트 선택 영역과 단일·다중 PNG가 같은 결과 캔버스를 사용하고 flip을 즉시 반영하는지 확인한다.
- 최소 높이 창에서 PNG 본문을 끝까지 스크롤해 목록·파일 추가·가져오기 안내에 접근할 수 있는지 확인한다.
- 1×에서는 바깥 세로 스크롤, 1.5×~8×에서는 이미지 pan과 원본 crop 좌표 보존을 확인한다.
- 확대·축소 버튼의 frame·아이콘·hit area가 같은지 실제 화면에서 비교한다.

## 진행 로그

- 2026-08-24: 사용자에게서 결과 프레임 경계 부족, PNG 결과 미리보기 부족, 확대 버튼 높이 불일치와 PNG 전체 스크롤 불가를 확인했다.
- 2026-08-24: 단일·다중 PNG와 스프라이트 선택 결과에 공통 투명 격자·파란 저장 경계를 적용하고, PNG 본문 전체 세로 스크롤과 1×/확대 pan 소유권 분리를 구현했다.
- 2026-08-24: 최소 860×680 실제 앱 창의 상단·하단 스크롤 상태를 창 단위로 확인해 고정 header/footer, 동일한 확대 버튼 높이, 결과 미리보기와 `PNG 더 추가…` 접근을 검토했다.
- 2026-08-24: Debug 빌드 성공, macOS 단위 테스트 467개 중 466개 성공·1개 기존 환경 조건 건너뜀·실패 0개를 확인했다.

## 완료 결과

- 스프라이트 선택 영역과 PNG crop 결과가 같은 투명 격자, 바깥 캔버스 테두리와 파란 실제 저장 범위를 표시한다.
- 단일 PNG도 결과 미리보기를 항상 제공하고 다중 선택은 별도 가로 결과 목록에서 집중 대상을 바꿀 수 있다.
- PNG 편집기는 header/footer를 고정하고 두 열 본문 전체를 세로 스크롤하며, 1×에서 내부 pan을 제거해 바깥 페이지 스크롤과 충돌하지 않는다.
- 확대·축소 버튼의 버튼 frame과 아이콘 frame을 통일했다.
- D-089, README, 플랫폼 현황, Windows 인계·실행 프롬프트와 배포 보류 문서를 갱신했다.

## 남은 위험 / 후속 작업

- 다음 릴리스 버전과 빌드 번호는 남은 수정 요청 전체가 확정된 뒤 올린다.
- Windows는 이 문서와 갱신된 인계 문서를 pull한 뒤 별도 작업 공간에서 구현한다.
- 현재 맥의 Xcode UI Test Runner는 앱 연결 전 bootstrap 단계에서 종료되어 새 UI 회귀 테스트를 자동 실행하지 못했다. 테스트 소스는 컴파일됐고 실제 Debug 앱 창으로 동일 시나리오를 수동 검토했으며, 다음 릴리스 전 Xcode 자동화 환경을 다시 확인한다.
