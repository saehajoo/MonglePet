# 행동 중심 실행 모델과 자동 동작 UI 재구성

## 상태

- 상태: in_progress
- 생성일: 2026-08-26
- 마지막 갱신: 2026-08-27

## 목표

- 자동·직접 선택, 이동 방향과 쓰다듬기에서 사용자 기능이 애니메이션을 직접 참조하지 않고 안정적인 ID의 행동을 참조하게 한다.
- 행동 편집과 자동 동작 결정을 서로 다른 설정 화면으로 분리해 `행동 루틴` 중심 용어와 생성 흐름의 혼동을 줄인다.
- 기존 macOS schema-v11 설정과 권장 프로필 schema-v7을 데이터 손실 없이 새 계약으로 변환한다.
- 모든 펫에서 원본을 보존하는 편집 가능한 새 사본 만들기를 제공한다.

## 범위

- 행동 Domain의 안정적 ID와 변경 가능한 표시 이름
- 이동 모드·방향별 행동과 쓰다듬기 행동 참조
- 이동 행동의 별도 event-driven playback 계층과 목록 우선순위 기반 좌표 이동 제어
- 자동/직접 선택 모드, 단일 입력 없음 규칙, 복수 앱 규칙과 이동을 포함한 목록형 우선순위
- macOS 설정 schema-v14와 권장 프로필 schema-v10, 이전 버전 마이그레이션과 fixture
- 따라가기·자유 이동·도망가기 및 도망가기 평상시 자유 이동의 모든 값 독립 저장
- 패키지 저장에 행동·이동·쓰다듬기·말풍선·휴대 표시 옵션 전체 포함
- 192pt=100%·10~200% 펫 크기 UI, 직접 입력 가능한 고정/랜덤 자유 이동 머무르기
- 별도 생성 sheet와 선택 행동 shuffle bag 기반 랜덤 행동 모드
- 행동·자동 동작·표시 및 이동 SwiftUI 재구성
- 내장·가져온·편집 가능한 펫의 새 편집 사본 생성
- 편집 가능한 펫의 선택 애니메이션 전체 복제와 Windows 인계
- 단일 복제본 편집·행동 연결 저장 경계와 고정 하단 오류 안내
- 현재 펫의 모든 애니메이션 프레임 조회·클릭 순서 조합과 독립 atlas 저장
- 애니메이션 공통 프레임 편집의 좌우·상하 반전, 방향 초기화와 복제 노출
- 공통 명세·결정·플랫폼 동등성·Windows 인계 문서

## 제외 범위

- Windows 소스 변경·Windows 빌드·실기기 QA
- 새 macOS Preview 버전·GitHub Release 게시
- 자동 업데이트와 업데이트 확인

## 열린 질문

- 없음. 사용자 검토를 통해 행동 중심 참조, 자동 동작 화면 이동, 테스트 단계의 즉시 스키마 변경과 새 기본 펫 후속 전달을 확정했다.

## 결정사항

- 애니메이션은 프레임 자산으로 남고 행동 단계만 애니메이션을 참조한다. 사용자 기능은 행동 ID를 참조한다.
- 이동 행동은 기본 행동의 시간축을 계속 진행시키는 별도 표시 계층이며 이동 시작·종료 또는 해석 행동 ID 변경 때만 스케줄러를 갱신한다.
- 쓰다듬기 행동은 가장 높은 표시 우선순위의 일회성 상호작용으로 실행한다.
- 이동·쓰다듬기 행동 변화는 행동 시작 말풍선을 발생시키지 않는다.
- 입력 없음 규칙은 프로필당 하나만 두고 사용 여부로 제어한다. 앱 규칙은 앱 식별자당 하나만 둔다.
- 자동 동작 우선순위는 개별 숫자 대신 `표시 및 이동`, `입력 없음 규칙`, `앱 사용 규칙` 세 항목의 위에서 아래 목록 순서로 해석한다.
- 일치한 앱·입력 없음 규칙이 `표시 및 이동`보다 앞이면 현재 위치에서 이동을 멈추고, 규칙 해제 후 현재 위치를 기준으로 다시 시작한다. `표시 및 이동`이 앞이면 이동을 유지한다.
- 입력 없음 규칙은 첫 생성 버튼 대신 항상 보이는 사용 토글로 켜고 끄며 시간·행동은 별도 `변경` 버튼으로 저장한다. 입력 없음 섹션을 앱 사용 규칙 추가보다 먼저 표시한다.
- 입력 없음 임계값은 `1...86,400초` 정수로 편집하며 기존 `idleAtLeast.milliseconds` 저장 계약에는 초 값의 1,000배를 기록한다.
- 기존 애니메이션 직접 참조는 같은 애니메이션별 단일 단계 행동을 중복 없이 생성해 변환한다.
- 릴리스와 새 내장 펫 교체는 새 펫 파일을 받은 뒤 별도 완료 단계로 진행한다.
- 랜덤은 애니메이션 단계가 아니라 사용자가 고른 행동 단위를 섞고, 한 bag에서 각 행동을 한 번씩 실행한다.
- 자유 이동 랜덤 시간은 목표 도착 시 한 번만 추첨하며 기존 시간은 최댓값으로 유지한다.
- 10~200% 크기는 기존 pt 저장을 유지하고 25% 미만에서 찾기·드래그 복구 안내를 제공한다. `설정 대상 펫`과 `펫 깨우기`는 별도 행으로 두고 세부 표시·정지 반경·방향별 행동도 접지 않고 항상 보여준다.
- schema-v14에서 각 이동 방식은 필요한 수치·시간 방식·행동을 독립 소유하며 도망가기의 평상시 자유 이동도 일반 자유 이동 값을 재사용하지 않는다.
- 권장 프로필 v10과 앱의 패키지 저장은 모든 휴대 펫 옵션을 포함한다. 화면 좌표·디스플레이·모든 펫 공통 이동 범위·깨움·로그인 실행은 제외하고 앱 규칙만 별도 선택한다. v9 이하 적용은 현재 표시 설정을 유지한다.
- 애니메이션 복제는 하나의 진입점에서 복사본 편집 화면을 열고, 행동 연결은 해당 화면의 공통 선택을 저장할 때만 적용한다. 취소 시 복사본을 되돌리고 오류는 하단 작업 영역에서 항상 보이게 한다.

## 작업 순서

### 공통 계약

- [x] 1단계: 행동·자동 동작·이동·쓰다듬기 계약과 제품 결정을 문서화한다.
- [x] 2단계: schema-v12, 권장 프로필 schema-v8과 공통 마이그레이션 fixture를 확정한다.

### macOS

- [x] 3단계: Domain·설정 편집기·저장 DTO와 v11→v12 마이그레이션을 구현한다.
- [x] 4단계: 기본·이동·쓰다듬기 행동 playback과 표시 우선순위를 구현한다.
- [x] 5단계: 행동·자동 동작·표시 및 이동 UI와 새 행동 생성 흐름을 구현한다.
- [x] 6단계: 모든 펫의 새 편집 사본 생성과 선택 인스턴스 연결을 구현한다.
- [x] 7단계: 권장 프로필 v8 내보내기·가져오기·요약을 구현한다.
- [x] 7A단계: schema-v13·권장 프로필 v9, 퍼센트 크기, 랜덤 머무르기, 새 행동 sheet와 랜덤 행동 모드를 구현한다.
- [x] 7B단계: 애니메이션 추가·수정에서 새 행동 또는 기존 행동 단계 연결을 저장 경계에 구현한다.
- [x] 7C단계: schema-v14 독립 이동과 권장 프로필 v10 전체 옵션 내보내기·가져오기를 구현한다.
- [x] 7D단계: 단일 애니메이션 복제·행동 연결 저장 경계와 하단 오류 안내를 구현한다.
- [ ] 8단계: 대상 테스트, 전체 단위 테스트, Debug 빌드와 실제 앱 QA를 수행한다.

### Windows

- [x] 9단계: 공통 계약·fixture·macOS 기준 동작과 Windows 네이티브 구현 지침을 인계 문서에 기록한다.
- [ ] 10단계: Windows 환경에서 schema·Domain·런타임·WinUI를 구현하고 검증한다.
- [ ] 10A단계: macOS 정보 구조·시각 위계와 최신 편집 UI를 WinUI 네이티브 컨트롤로 가깝게 재현하고 DPI·테마·접근성을 검증한다.
- [ ] 10B단계: 마우스 도망가기의 평상시 자유 이동에서 매 tick 목표가 초기화되는 회귀를 수정하고 목표 수명 테스트와 실제 이동 QA를 수행한다.

### 플랫폼 동등성

- [ ] 11단계: 양쪽 플랫폼의 공통 fixture 왕복, 사용자 시나리오와 성능 QA를 확인한다.

### 기본 펫과 릴리스

- [x] 12단계: 새 기본 펫 파일을 받은 뒤 최종 행동·이동·쓰다듬기 권장 설정을 적용한다.
- [ ] 13단계: 새 macOS Preview 버전과 GitHub Release를 검증·게시한다.

## 검증 방법

- v11→v12와 권장 프로필 v1~v8 fixture 마이그레이션·왕복
- 같은 애니메이션의 자동 생성 행동 중복 방지와 참조 복구
- 자동/직접 선택, 단일 입력 없음과 앱 규칙 종류 우선순위 Domain 테스트
- 이동 중 같은 행동 ID의 재시작 방지, 방향 행동 변경, 정지 복귀와 쓰다듬기 우선순위
- 이동 33ms cadence 중 행동 timer·playback 변경이 상태 변경 횟수로 제한되는지 확인
- Windows 도망가기 평상시 자유 이동의 목표가 도착 전 100회 tick 동안 유지되고 도망 해제 전환과 dwell 종료 때만 다시 생성되는지 확인
- macOS·Windows 동일 시나리오 스크린샷의 정보 순서·카드 위계·버튼 크기·행 hit area·미리보기 배치 비교와 Windows DPI·테마·Narrator QA
- 펫 복제 뒤 원본 패키지·프로필 불변성과 새 설치·프로필·선택 인스턴스 연결
- 애니메이션 복제의 고유 이름, 원본 atlas 공유, 수정 시 분리, 취소 rollback과 저장 시 행동 연결 한 번 적용
- 현재 펫 프레임 조합의 클릭 순서·원본 간격과 원본 애니메이션 삭제 뒤 독립 보존
- 공통 프레임 편집의 비대칭 픽셀 양축 flip, 배치 기준점과 복제 상태 보존
- 권장 프로필 내보내기·가져오기 전체 왕복과 미래·손상 schema 격리
- 자유 이동과 도망가기 평상시 자유 이동의 서로 다른 시간 방식·범위·속도 왕복 및 v13→v14 복제 마이그레이션
- v10 휴대 표시 전체 적용과 화면 좌표·공통 이동 범위 보존, v9 이하 표시 미적용
- 전체 `MonglePetTests`, 코드서명 없는 Debug 빌드, 실제 설정 UI와 오버레이 수동 QA

## 진행 로그

- 2026-08-26: 현재 행동·이동·쓰다듬기 런타임, schema-v11, 권장 프로필 v7과 설정 UI를 검토하고 작업 계획을 시작했다.
- 2026-08-26: schema-v12·권장 프로필 v8, 행동 중심 이동·쓰다듬기 runtime, 자동 동작 UI, 모든 펫 사본 생성과 Windows 인계 문서를 구현했다.
- 2026-08-26: macOS 단위 테스트 475개 중 474개 성공·선택형 WebP fixture 1개 건너뜀·실패 0개와 코드 서명 없는 Debug 빌드를 통과했다. 실제 설정 UI·오버레이 QA는 사용자 확인 단계로 남겼다.
- 2026-08-26: 전체 UI 테스트와 핵심 설정 화면 한 건을 각각 시도했으나 두 번 모두 테스트 코드 진입 전 `MonglePetUITests-Runner`가 signal kill로 종료되어 부트스트랩하지 못했다. 이는 `TESTING.md`에 기록된 Codex 활성 세션의 기존 XCUITest 제한과 같은 양상이다. 임시 설정 격리와 UI 테스트 소스 컴파일은 확인했으며 Codex를 비활성화한 Xcode 직접 실행과 실제 앱 QA를 후속으로 남겼다.
- 2026-08-26: 자동 동작 표시 우선순위를 `표시 및 이동`, `입력 없음 규칙`, `앱 사용 규칙`의 위·아래 정렬 목록으로 확장했다. 입력 없음 규칙은 항상 보이는 사용 토글과 변경 버튼으로 편집하고 앱 규칙 추가보다 먼저 배치했다. 전체 단위 테스트 479개 중 478개 성공·선택형 WebP fixture 1개 건너뜀·실패 0개와 코드 서명 없는 Debug 빌드를 통과했다.
- 2026-08-26: 목록의 의미를 실제 이동까지 확장했다. 일치 규칙이 `표시 및 이동`보다 앞이면 이동 lifecycle이 timer·목표·이동 행동을 정리하고 현재 위치에서 멈추며, 규칙 해제 후 현재 위치에서 다시 시작한다. 전체 단위 테스트 480개 중 479개 성공·선택형 WebP fixture 1개 건너뜀·실패 0개와 코드 서명 없는 Debug 빌드를 통과했다.
- 2026-08-26: 편집 가능한 펫에 선택 애니메이션 전체 복제를 추가했다. 다음 고유 `복사본 N` 이름으로 원본 바로 뒤에 넣고 새 항목을 선택해 수정 화면을 열며, 원본 atlas 공유 후 복사본 수정 시 독립 atlas로 분리한다. 기본 애니메이션과 행동 참조는 승계하지 않는다. 전체 단위 테스트 482개 중 481개 성공·선택형 WebP fixture 1개 건너뜀·실패 0개와 코드 서명 없는 Debug 빌드를 통과했다.
- 2026-08-26: 편집 가능한 현재 펫의 모든 애니메이션 프레임을 그룹별로 조회해 클릭 순서와 원본 간격으로 새 애니메이션에 조합하고 새 전용 atlas로 저장하는 선택기를 추가했다. 공통 프레임 편집에는 출처와 관계없이 양축 flip·방향 초기화·복제를 노출했다. 대상 편집기 테스트 16개와 전체 단위 테스트 484개 중 483개 성공·선택형 WebP fixture 1개 건너뜀·실패 0개를 확인했다. 실제 앱 QA는 후속 검증으로 남겼다.
- 2026-08-26: 양축 flip 뒤 선택 프레임 직접 배치 미리보기가 원본 content를 계속 표시하던 경로를 반전 content cache로 통일했다. 조작부는 위치·이미지 방향·프레임 작업으로 구분하고 가로·세로 증감 버튼을 같은 크기의 가로형 두 축 컨트롤로 재배치했다. 대상 편집기 테스트 16개와 코드 서명 없는 Debug 빌드를 통과했다.
- 2026-08-26: 애니메이션 추가·수정의 `프레임 추가` 메뉴를 오른쪽 `프레임 순서와 간격` 제목으로 옮기고 빈 초안에서는 빈 상태 위에 유지했다. 펫 보관함 애니메이션은 목록과 버튼을 전체 폭으로 확장하고 행 전체 hit area를 적용했다. 코드 서명 없는 Debug 빌드를 통과했다.
- 2026-08-26: 현재 펫 프레임 선택기의 그룹·순서 strip·재생 미리보기는 원본 저장 이미지를 유지한 채 알파 content 표시본만 가운데 정렬하도록 분리했다. 흰색이 강한 투명 격자는 시스템 control 배경 기반의 낮은 대비 패턴으로 교체했다. 대상 편집기 테스트 16개가 통과했다.
- 2026-08-26: 상태 메뉴의 저빈도 `안전 모드로 전환…`을 `설정…` 옆 직접 항목에서 구분된 `문제 해결` 하위 메뉴로 옮겼다. 안전 모드 동작은 유지하고 의도적인 2단계 진입으로 오선택 가능성을 줄였으며 메뉴 대상 테스트 1개를 통과했다.
- 2026-08-26: Windows가 macOS 최신 정보 구조와 시각 위계를 WinUI 방식으로 가깝게 재현하도록 화면별 대조·DPI·테마·접근성 기준을 인계했다. Windows `TickCursorAvoiding`이 평상시 자유 이동 목표를 매 tick 지우는 원인을 확인하고, 도망 해제 전환 때만 목표를 초기화하도록 하는 수정·회귀 테스트·실제 QA를 10A·10B 단계에 추가했다.
- 2026-08-26: 펫 크기를 192pt=100%의 50~200% UI로 바꾸고 직접 입력·초기화를 추가했다. 자유 이동 머무르기는 직접 초 입력과 고정/최소~최대 랜덤 범위를 제공하며 도착 시 한 번만 추첨한다. 새 행동은 이름·첫 애니메이션·반복을 정하는 sheet에서 만들고, 자동 동작에는 선택 행동을 shuffle bag으로 한 번씩 재생하는 `랜덤 선택`을 추가했다. 로컬 설정 schema-v13·권장 프로필 v9와 Windows 인계를 갱신했다.
- 2026-08-26: 펫 크기 하한을 10%(19.2pt)로 완화하고 10·25·50·100·150·200% 빠른 선택과 25% 미만 복구 안내를 추가했다. `표시 및 이동`은 현재 펫·깨우기 헤더와 기본/고급 표시·이동 설정, 모든 펫 공통 이동 범위로 위계를 정리했다. 고정/랜덤 머무르기는 같은 크기의 0.5초 증감·직접 입력으로 재구성하고 값 변경이 재실행 뒤에도 유지되도록 저장 경로를 보완했다.
- 2026-08-26: 사용자 확인에 따라 첫 섹션을 `설정 대상 펫`과 별도 `펫 깨우기` 행으로 되돌리고 표시·정지 반경·방향별 행동의 접힘 UI를 제거했다. 퍼센트와 초 입력은 단위를 필드 안에 고정한 동일 형태로 조정하고 머무는 시간의 제목·방식·값 행을 분리했다.
- 2026-08-26: 애니메이션 전체 복제를 `애니메이션만 복제`와 `복제하고 새 행동 만들기`로 나눴다. 후자는 복사본 애니메이션 한 단계의 반복 행동을 고유 이름으로 추가하고 기존 행동 참조는 유지한다.
- 2026-08-26: 복제 애니메이션으로 새 행동을 만드는 대상 테스트와 전체 단위 테스트 491개 중 490개 성공·선택형 WebP fixture 1개 건너뜀·실패 0개를 확인했다.
- 2026-08-26: 복제와 함께 새 행동을 만들 때 같은 이름을 피한 결과나 행동 생성 실패를 펫 보관함 상단 공통 오류 대신 작업 직후 alert로 표시하고, 확인 뒤 복사본 수정 화면을 열도록 보완했다.
- 2026-08-26: 복제 직후 수정 화면의 저장을 확정 경계로 바꾸고 취소·창 닫기 시 복사본 애니메이션과 선택적으로 생성한 행동을 함께 제거하도록 보완했다.
- 2026-08-26: 애니메이션 추가·수정 화면에 `연결 안 함`, 한 단계 `새 행동 만들기`, `기존 행동에 추가`를 공통 제공했다. 수정 화면은 현재 사용 행동을 표시하고, 같은 애니메이션 중복 단계는 안내 뒤 허용하며 취소 전에는 행동을 바꾸지 않는다. 특정 애니메이션 단계 추가 Domain·세션 대상 테스트와 전체 단위 테스트 492개 중 491개 성공·선택형 WebP fixture 1개 건너뜀·실패 0개, 앱 컴파일을 통과했다.
- 2026-08-26: 표시 및 이동의 쓰다듬기·공통 이동·방향별 행동 선택기는 행동 이름을 섞지 않고 현재 애니메이션 이름, 기본 참조 또는 `첫 애니메이션 외 N개`만 표시하도록 정리했다. 애니메이션 저장 시 아직 자동 생성 `복사본` 형태인 한 단계 행동 이름을 현재 애니메이션에 동기화하되 사용자 지정·여러 단계·이름 충돌은 보존하며 행동 ID와 참조는 유지한다. 자유 이동 머무는 시간은 고정 높이 사용자 정의 필드를 제거하고 네이티브 숫자 필드와 별도 `초` 단위로 바꿔 숫자 세로 잘림을 방지했다. 대상 행동 편집기 테스트 10개와 전체 단위 테스트 494개 중 493개 성공·선택형 WebP fixture 1개 건너뜀·실패 0개를 확인했다. 실제 좁은 창 QA는 후속 검증한다.
- 2026-08-26: 자동 동작 입력 없음 규칙을 분 단위에서 `1...86,400초` 정수 입력과 1초 증감으로 바꿨다. 기존 분 단위 값은 같은 시간의 초 값으로 읽고 저장 필드·schema는 유지한다. Domain 편집 경계는 1초와 24시간을 허용하고 범위 밖을 거부하도록 갱신했다. 대상 단위 테스트 40개와 전체 단위 테스트 494개 중 493개 성공·선택형 WebP fixture 1개 건너뜀·실패 0개, 코드 서명 없는 Debug 빌드를 통과했다. UI 테스트 번들은 컴파일됐지만 runner가 앱 연결 전에 두 번 종료되어 실제 초 표시는 사용자 QA로 남겼다.
- 2026-08-26: 10% 하한·설정 저장 대상 테스트와 전체 단위 테스트 490개 중 489개 성공·선택형 WebP fixture 1개 건너뜀·실패 0개, 코드 서명 없는 Debug 빌드와 `git diff --check`를 통과했다. 좁은 설정 창, 실제 10% 펫 hit area와 VoiceOver는 실제 앱 QA로 남겼다.
- 2026-08-26: schema-v13 공통 fixture 왕복, v12 마이그레이션, 권장 프로필 v9, 랜덤 행동 경계와 랜덤 머무르기 대상 테스트를 통과했다. 전체 단위 테스트 489개 중 488개 성공·선택형 WebP fixture 1개 건너뜀·실패 0개, 코드 서명 없는 Debug 빌드와 `git diff --check`를 통과했다. 새 UI의 실제 앱 QA는 사용자 확인 단계로 남겼다.
- 2026-08-26: 사용자 제공 `몽몽글.monglepet`(SHA-256 `fe11845090381222142bdf05bd4e1ec355e8e103ab931c03cade79d9cfd2c48e`)을 경로·압축·실행 파일·픽셀 한도와 앱 패키지 로더로 검증했다. 내장 예약 ID를 유지하고 펫 버전 `1.0.2`, 제작자 `운영자`, 기본 제공 설명으로 공통 패키지와 macOS runtime을 12개 모션·51프레임으로 교체했다. 권장 프로필 v9의 기본 행동 `정면`, 50초 입력 없음 `자는 중`, 이동 우선순위·마우스 도망가기·랜덤 머무르기·4방향 행동·쓰다듬기 `해피`를 그대로 적용했다. 종전 1.0.1 기본 프로필과 정확히 같은 설정만 새 기본값으로 승격하고, 사용자 수정 프로필은 행동 ID·구조를 보존한 채 사라진 모션 이름만 새 이름으로 연결한다. 전체 단위 테스트 496개 중 495개 성공·선택형 WebP fixture 1개 건너뜀·실패 0개, 코드서명 없는 Debug 빌드와 사용자 데이터 격리 5초 마우스 도망가기 실행·정상 종료를 확인했다. 실제 방향·쓰다듬기·50초 입력 없음 전환과 설정 화면 메타데이터는 릴리스 전 실제 앱 QA로 남겼다.
- 2026-08-26: 로컬 schema-v14에서 마우스 따라가기·자유 이동·도망가기와 도망가기 평상시 자유 이동의 수치·시간 방식·행동을 독립 저장하도록 분리했다. 권장 프로필 v10과 패키지 저장에는 앱 규칙을 제외한 모든 휴대 펫 설정을 항상 포함하고, 전체 적용은 행동과 표시를 한 번에 저장하면서 화면 좌표·공통 이동 범위를 보존한다. v9 이하에는 표시 설정이 없음을 구분해 현재 표시값을 유지한다. 관련 저장·마이그레이션·패키지·세션 테스트 82개를 통과했고, 전체 단위 테스트 500개 중 499개 성공·선택형 WebP fixture 1개 건너뜀·실패 0개와 코드 서명 없는 Debug 빌드를 확인했다.
- 2026-08-27: 패키지 공유·가져오기 공통 요약에서 내부 행동 ID를 표시 이름으로 바꾸고 행동 모드·자동 규칙 종류 우선순위와 따라가기·자유 이동·도망가기·도망가기 평상시 자유 이동의 독립 수치·시간 방식·행동을 모두 확인할 수 있게 했다. v10 표시 설정을 제외 항목으로 잘못 안내하던 문구와 설치 버튼의 적용 범위를 바로잡았다. 전체 단위 테스트 500개 중 499개 성공·선택형 WebP fixture 1개 건너뜀·실패 0개와 코드 서명 없는 Debug 빌드를 통과했다. 실제 대화상자 스크롤·가독성은 앱 QA로 남겼다.
- 2026-08-27: 사용자 제공 `최종.monglepet`(SHA-256 `d33c6265475969278012b96d4906311d73891c68b1b13c8e0464ac0bbaf47f9b`)을 내장 몽글이 `1.0.3`으로 반영했다. 13개 모션·53프레임과 권장 프로필 v10의 60초 입력 없음·2~6초 랜덤 머무르기·좌우 보글보글 방향 행동·쓰다듬기 행복·50% 휴대 표시를 공통 패키지와 macOS runtime에 맞췄다. 정확히 1.0.2 기본값인 경우만 새 행동·표시값으로 승격하고 수정값 보존 테스트를 추가했다.
- 2026-08-27: 애니메이션 복제의 두 외부 선택을 `애니메이션 복제…` 하나로 합치고 원본이 표시되는 복제본 편집 화면의 공통 `행동 연결`을 유일한 선택 지점으로 만들었다. 저장 전에 행동을 만들지 않고 취소 시 복사본을 제거하며, 복제 실패 alert와 하단 고정 검증·저장 오류 banner를 추가했다. Windows 인계와 사용자 README도 같은 흐름으로 갱신했다.
- 2026-08-27: 내장 몽글이 1.0.3과 단일 복제 흐름을 포함한 전체 단위 테스트 503개 중 502개 성공·선택형 WebP fixture 1개 건너뜀·실패 0개, 코드 서명 없는 Debug 빌드와 `git diff --check`를 통과했다. 실제 애니메이션·방향 전환과 복제 dialog 오류 안내는 릴리스 전 앱 QA로 남겼다.

## 완료 결과

- 진행 중

## 남은 위험 / 후속 작업

- 새 기본 펫 자산·애니메이션·행동 매핑은 macOS와 공통 패키지에 확정됐으며 Windows 네이티브 반영은 인계 문서를 따른다.
- 행동·자동 동작·표시 및 이동 화면과 이동·쓰다듬기 표시 우선순위는 실제 앱에서 최종 확인한다.
- Windows의 현재 `마우스 도망가기 + 평상시 자유 이동`은 목표 수명 회귀가 확인됐으므로 10B 수정과 실제 Release QA 전에는 완료로 판단하지 않는다.
- Windows 구현과 실기기 QA가 끝나기 전에는 플랫폼 동등 완료로 표시하지 않는다.
