# MonglePet 테스트 전략

## 원칙

- 도메인 규칙은 운영체제 API 없이 빠른 단위 테스트로 검증한다.
- 시스템 API와 파일 시스템은 adapter 경계에서 통합 테스트한다.
- UI 테스트는 핵심 사용자 흐름과 앱 실행 가능 여부에 집중한다.
- 코드 변경 후 가장 좁은 관련 테스트부터 실행하고 영향 범위에 따라 검증을 확장한다.
- 시간 기반 로직은 실제 대기 대신 주입 가능한 시계로 검증한다.

## 기본 빌드

```sh
xcodebuild -project apps/macos/MonglePet.xcodeproj \
  -scheme MonglePet \
  -configuration Debug \
  -destination 'platform=macOS' \
  -derivedDataPath /tmp/MonglePetDerivedData \
  CODE_SIGNING_ALLOWED=NO \
  build
```

## 단위 테스트

```sh
xcodebuild -project apps/macos/MonglePet.xcodeproj \
  -scheme MonglePet \
  -configuration Debug \
  -destination 'platform=macOS' \
  -derivedDataPath /tmp/MonglePetDerivedData \
  CODE_SIGNING_ALLOWED=NO \
  test -only-testing:MonglePetTests
```

주요 단위 테스트 대상:

- 자동·수동 모드 우선순위
- 입력 없음 시간 경계값과 입력 재개 시 즉시 규칙 해제
- 앱 규칙 매칭과 기본 폴백
- 애니메이션 사이클·행동 목록의 반복 횟수와 전환 경계
- schema-v5 펫별 방향 이동 설정·`repeatCount` 저장 왕복과 schema-v1·v2·v3·v4 순차 마이그레이션
- 커서 거리·화면 경계·다중 모니터·선호 창 폴백과 속도 기반 이동 좌표
- 실제 적용 이동 벡터의 4·8방향 분류, 각도 히스테리시스와 방향→기본 축→공통 모션 fallback
- 명시적인 자동 규칙 조건 JSON discriminator
- 현재 펫 기본 참조와 누락된 애니메이션의 선언된 기본 애니메이션 대체
- 설치 UUID별 행동 모드·수동 선택·루틴·자동 규칙의 독립 저장과 전환 복원
- 같은 설치 펫의 여러 활성 인스턴스에서 행동·말풍선·쓰다듬기·깨움 상태와 네 이동 모드가 서로 영향을 주지 않고, 공유 activity snapshot을 각 인스턴스 규칙으로 독립 판정하는지 확인
- 같은 설치 교체·별도 사본·명시적 삭제·연결이 끊긴 행동 설정의 생명주기
- 공유 `.monglepet`의 정규화 manifest·참조 자산 선별·marker 제외·ZIP 왕복과 원자적 저장
- 권장 프로필 schema-v7 말풍선 정책·표시 방식·테마·상대 배치 전체 필드 왕복과 schema-v1~v6 호환, 내보내기·가져오기 공통 요약
- 공유 권한 확인 요구, 메타데이터 검토 후 변경 시 재확인과 레거시 `license` 무시
- 내장 펫 공유 제외, 선택 설치 펫 검토 전달, 내보내기 진행 상태와 오류 전달
- 패키지 경로, 크기, 이미지와 manifest 검증
- Codex legacy v1 8×9 및 v2 8×11 WebP atlas 매핑
- 정적 WebP와 animated WebP 판별 및 미지원 형식 거부
- 손상된 설정 복구와 스키마 마이그레이션

## UI 테스트

```sh
xcodebuild -project apps/macos/MonglePet.xcodeproj \
  -scheme MonglePet \
  -configuration Debug \
  -destination 'platform=macOS' \
  -derivedDataPath /tmp/MonglePetDerivedData \
  test -only-testing:MonglePetUITests
```

UI 테스트는 앱 실행과 접근성 자동화가 가능한 macOS 세션에서 실행한다. 기본 스모크 테스트는 앱 실행, 메뉴 막대 항목 또는 펫 창 노출, 핵심 설정 화면 진입 여부를 확인한다.

## 통합 테스트

- 펫 설치와 재실행 후 복원
- 전면 앱 전환에 따른 행동 변경
- 사용자 입력 없음 규칙 진입과 입력 재개 후 다음 snapshot의 즉시 해제
- 화면 잠금·절전에서 렌더링 중지
- 설정 변경의 즉시 반영
- 펫 제거 시 파일과 설정 정리
- 펫 전환 시 행동 루틴·자동 규칙 화면의 설정 대상 펫 표시와 편집 내용 분리
- 원자적 설정 저장과 손상 파일 복구

## 수동 QA

- 다중 모니터와 화면 배율 변경
- 전체 화면 앱과 여러 Space
- 클릭 통과와 드래그
- 로그인 직후 실행
- 동작 줄이기 설정
- 매우 크거나 잘못된 외부 패키지
- 펫을 전환하며 행동 루틴·자동 규칙 탭의 설정 대상 이름과 각 펫의 편집 내용 확인
- lossless/lossy WebP와 알파가 없는 WebP 가져오기 결과
- 같은 크기의 셀로 구성된 투명 WebP를 가져온 뒤 원본 셀 크기와 캐릭터 위치가 유지되는지 확인
- 일반 설정의 `픽셀 아트 선명하게`를 켜고 끌 때 픽셀 아트와 일반 일러스트 표시를 각각 비교
- 장시간 실행과 절전 복귀
- 밝은·어두운 화면에서 말풍선 preset·사용자 색상 대비, 긴 대사 레이아웃, 자동·위·아래 위치와 좌우 오프셋·간격·꼬리 방향

## 성능 검증

- 펫 숨김과 화면 잠금 상태에서 애니메이션 시계가 중지되는지 확인한다.
- 일반 애니메이션 평균 CPU 1% 미만을 초기 목표로 측정한다.
- 설정창을 닫은 뒤 관련 UI 메모리가 해제되는지 확인한다.
- 멀티펫 필수 장시간 성능 검증은 실제 Release 대표 이동 workload를 5분 실행해 5초 간격으로 측정한다. 전체 시스템 환산 평균 CPU 5% 미만, private memory 200MiB 미만·시작 대비 증가 10MiB 미만, 무응답·충돌 0회를 Windows 초기 합격 기준으로 사용한다. 1시간 soak는 공개 배포 전 또는 누수 징후가 있을 때 선택적으로 확장한다.
- 이미지 디코딩과 프레임 캐시의 최대 메모리 사용량을 큰 테스트 패키지로 측정한다.
- Phase 10에서는 독립 실행한 Release 빌드로 마우스 따라가기 2분·정지 1분·자유 이동 2분·대기 1분만 먼저 측정한다.
- 이동 중 CPU 10% 이상 또는 정지·대기 중 CPU 1~2% 이상이 지속될 때만 Time Profiler 분석과 최적화를 진행한다.

### Windows 오버레이 선행 성능 실험

- Windows 기능 구현을 확장하기 전에 C# Win32 `HWND`와 `Microsoft.UI.Composition`으로 실제 투명 펫 오버레이를 먼저 만든다.
- 실제와 같은 프레임 크기·알파 이미지·말풍선을 사용해 고정, 마우스 따라가기, 자유 이동, 마우스 도망가기와 다중 모니터 전환을 독립 실행 Release 빌드에서 측정한다.
- 유휴·이동 상태의 CPU와 GPU, private memory, 프레임 지연, 입력 반응과 장시간 메모리 증가를 각각 기록한다.
- WPF layered window는 기준 구현이 아니라 Composition 경로의 수치 비교 또는 호환성 문제 재현이 필요할 때만 같은 workload로 실험한다.
- C#·Composition 경로가 확정한 기준을 만족하지 못하면 allocation, 이미지 디코딩·업로드, 타이머와 composition commit 횟수를 먼저 프로파일링한다.
- 관리형 최적화 후에도 병목이 남을 때만 해당 렌더링·디코딩·메모리 복사 구간의 C++/WinRT 모듈 실험을 추가한다.
- 가상 머신은 개발 점검에 사용할 수 있지만 성능·다중 모니터 최종 기준은 실제 Windows 하드웨어에서 확정한다.

#### Windows 고정 PNG 기준선 (2026-08-08)

- 환경: x64 Windows 11 25H2 build 26200.8875, 논리 프로세서 6개, .NET 10.0.302, Windows App SDK 2.3.1, packaged Release 빌드
- workload: 220×220 항상 위·클릭 통과 오버레이에서 공통 fixture의 1254×1254 투명 PNG 단일 프레임 표시, 5초 warm-up 뒤 30초 측정
- 결과: CPU 전체 시스템 기준 평균 0.017%, 단일 코어 환산 0.103%, private memory 평균·최대 100.2 MiB, working set 평균 144.9 MiB, 3D GPU 평균·최대 0%
- 첫 측정의 manifest 외 무한 scale 애니메이션은 전체 시스템 CPU 3.133%를 사용해 제거했다. 단일 프레임은 timer와 지속 Composition animation을 만들지 않는 것을 기준으로 한다.
- 판정: 고정 PNG workload의 CPU 1%·private memory 200 MiB 초기 기준 통과. 실제 다중 프레임·WebP, 이동·말풍선, 프레임 지연 p95와 1시간 메모리 증가는 미측정이다.

#### Windows 설정·펫 라이브러리 검증 (2026-08-08)

- x64 Debug·Release 전체 솔루션 빌드가 경고·오류 없이 통과했고 Core 8개, Packages 17개, PetLibrary 10개, Settings 24개로 총 59개 xUnit 테스트가 두 구성에서 모두 통과했다.
- Settings 테스트에서 schema-v10 기본 문서, 선택 UUID 왕복, 미사용 필드 보존, 잘못된 UUID 항목 복구, 손상·5MiB 초과 격리, v1부터 v9까지의 순차 마이그레이션, 미래 schema 원본 보호와 임시 파일 정리를 확인했다.
- macOS schema-v1 fixture를 공유해 focus·기본 모션의 유지 시간을 주입한 800ms·600ms 사이클로 각각 3회·2회 반복으로 변환하고, 누락 모션은 현재 펫 기본 모션 1회로 복구하는지 확인했다. 선택 펫 정의를 얻을 수 없는 v1은 원본과 쓰기 차단 상태를 유지한다.
- 실제 packaged Debug 앱의 `LocalState`에서도 같은 fixture를 v10으로 원자적 교체하고 반복 횟수 3·2·1, 이동 모드 `fixed`, 말풍선 배치 `automatic`, 임시 파일 0개와 앱 정상 응답을 확인했다. 단위 테스트에서는 마이그레이션된 파일을 다시 로드할 때 추가로 쓰지 않는 것도 확인했다.
- schema-v10 전체 Domain 매핑 테스트에서 overlay, 행동 루틴·자동 규칙, 이동·방향 모션, 쓰다듬기와 말풍선 대사·정책·테마·배치 왕복을 확인했다. 전체 저장 뒤 상위·overlay·프로필·루틴·규칙·조건·대사·트리거 확장 marker가 유지된다.
- 부분 손상 테스트에서 잘못된 enum·범위·참조·방향 모션·말풍선 대비·배치를 각각 복구하고, 잘못된 단계·규칙·대사·중복 프로필만 제외하거나 비활성화하며 로드만으로 원본을 다시 쓰지 않는 것을 확인했다.
- 101개 행동 루틴을 저장 순서의 앞 100개로 제한하고 typed issue를 반환하며, 유효하지 않은 Domain 전체 저장은 기존 파일과 임시 파일을 변경하지 않고 거부하는 것을 확인했다.
- typed schema-v10 로더를 포함한 Release loose AppX를 깨끗한 LocalState에 개발 등록해 앱 프로세스의 정상 응답을 확인했다.
- 실제 packaged Debug 앱의 `LocalState`에서 사라진 선택 UUID를 사용 가능한 설치 UUID로 복구하면서 미사용 JSON marker를 보존하고, 재실행 시 파일을 다시 쓰지 않고 같은 UUID를 복원하는 것을 확인했다.
- 설치가 없어진 다음 실행에서 선택 UUID를 `null`로 정리하고 bundled 샘플로 fallback했으며, 최종 Release packaged 앱이 깨끗한 데이터에서 정상 응답하는 프로세스로 시작하는 것을 확인했다.
- WinUI `.monglepet` 파일 선택기와 중복 교체·별도 설치, 목록 활성화·삭제 XAML은 Debug·Release 빌드로 검증했다. OS 파일 선택 대화상자의 실제 선택 조작은 수동 QA가 남아 있다.
- 화면 표시 typed 설정의 `tuckedAway`, 304px, 65%, 클릭 통과와 픽셀 렌더링 저장 왕복을 xUnit으로 확인했다. 실제 packaged Release 시작에서는 304×304 HWND가 `awake`에서 표시되고 `tuckedAway`에서 숨겨지며 `WS_EX_TRANSPARENT`가 복원되는 것을 확인했다.
- 실제 packaged Release WinUI를 UI Automation으로 조작해 272px·55%·클릭 통과·픽셀 렌더링 변경이 HWND와 LocalState에 반영되고 숨김 버튼이 `tuckedAway`와 비표시 상태를 함께 저장하는 것을 확인했다. QA 뒤 프로세스·패키지 등록·LocalState·임시 레이아웃이 모두 제거됐다.

#### Windows 행동 런타임과 기본 설정 검증 (2026-08-09)

- x64 Debug·Release 전체 솔루션 빌드가 경고·오류 없이 통과했고 Core 13개, Packages 18개, PetLibrary 10개, Settings 25개로 총 66개 xUnit 테스트가 두 구성에서 모두 통과했다.
- Core scheduler 테스트에서 모션 사이클, 단계별 `repeatCount`, 여러 단계·루틴 반복, 같은 요청의 진행 유지, 편집·다른 요청의 즉시 재시작, pause 중 시간 보존, 비반복 완료와 누락 모션 fallback을 확인했다.
- Packages 테스트에서 manifest가 비반복으로 선언한 모션도 행동 단계에서는 scheduler의 cycle 반복 지시에 맞춰 재생 상태가 순환하는 것을 확인했고, Settings 테스트에서 내장·설치 UUID별 기본 행동 프로필 생성을 확인했다.
- 두 모션 QA fixture를 포함한 실제 packaged Release WinUI를 UI Automation으로 조작해 자동 기본 루틴에서 수동 `focus` 루틴으로 즉시 전환되고 schema-v10 LocalState에 저장되는 것을 확인했다.
- 수동 `focus` 실행 중 숨김이 `tuckedAway`와 비표시 상태를 함께 저장하고, 앱을 완전히 종료·재실행해 숨김 상태와 수동 선택을 복원한 뒤 다시 표시하면 `focus`가 재개되는 것을 확인했다. 자동 모드로 돌아가면 기본 루틴을 재생하면서 수동 선택 `focus`는 보존됐다.
- QA 뒤 앱 프로세스, 임시 패키지 등록, LocalState와 임시 추출 폴더가 모두 제거된 것을 확인했다.

#### Windows 활동 감지와 자동 규칙 검증 (2026-08-09)

- x64 Debug·Release 전체 솔루션 빌드가 경고·오류 없이 통과했고 Activity 9개, Core 13개, Packages 18개, PetLibrary 10개, Settings 25개로 총 75개 xUnit 테스트가 두 구성에서 모두 통과했다.
- Activity 테스트에서 PFN·실행 파일명 정규화와 경로 제거, 화면 사용 가능 상태에서만 idle·전면 앱 source 조회, 음수 idle 보정, lock/unlock·suspend/resume 상태 전이를 확인했다.
- 실제 packaged Release에서 MonglePet을 `pfn:4b7e245f-a59a-4e0f-84d7-52b511356256_1z32rh13vfry6`, Notepad를 `pfn:microsoft.windowsnotepad_8wekyb3d8bbwe`로 감지하고 창 제목·경로를 상태에 노출하지 않는 것을 확인했다.
- MonglePet 전면 앱 규칙이 `focus`, Notepad로 전환한 뒤 2초 입력 없음 규칙이 `rest`를 선택하고 MonglePet 복귀 시 높은 우선순위의 앱 규칙으로 즉시 돌아오는 것을 확인했다.
- 오버레이 HWND에 Windows 세션 잠금·해제와 전원 suspend·resume 메시지를 보내 runtime·frame playback이 일시 정지·복귀하고, 펫 숨김 중 1초 activity polling이 멈추는 것을 확인했다. 실제 컴퓨터 잠금·절전 복귀 수동 QA는 공개 배포 전 검증으로 남긴다.
- QA 뒤 앱 프로세스, 임시 패키지 등록, LocalState와 임시 추출 폴더가 모두 제거된 것을 확인했다.

#### Windows 행동 루틴과 자동 규칙 편집기 검증 (2026-08-09)

- x64 Debug·Release 전체 솔루션 빌드가 경고·오류 없이 통과했고 Activity 9개, Core 13개, Packages 18개, PetLibrary 10개, Settings 37개로 총 87개 xUnit 테스트가 두 구성에서 모두 통과했다.
- Settings 편집기 테스트 12개에서 루틴 이름 정규화·중복·100개 한계, 기본 루틴 보호, 최소 한 단계, 단계 교체·이동·삭제, 반복 여부, 앱·입력 없음 규칙 생성·교체·삭제와 대상 참조를 확인했다.
- 실제 packaged Release WinUI에서 `focusRoutine`을 만들고 `rest×2 → focus×3`, 루틴 반복 끔으로 편집했으며 단계 순서 변경과 schema-v10 LocalState 저장을 확인했다.
- 현재 전면 앱 채우기로 MonglePet PFN을 입력해 `focusRoutine` 규칙을 만들자 runtime이 즉시 전환됐고, 같은 규칙을 비활성·우선순위 42·입력 없음 5분·기본 루틴으로 바꾼 뒤 다시 PFN 규칙으로 복원했다.
- 두 번째 규칙의 생성·삭제와 임시 루틴 삭제 시 연결 규칙 cascade를 확인하고, 완전 종료·재실행 뒤 설정 파일을 다시 쓰지 않은 채 루틴·단계·규칙과 즉시 실행 상태를 복원했다.
- 단계 편집 이벤트 중 목록을 동기 재생성할 때 발생한 WinUI native 충돌을 재현해 UI 큐 갱신으로 수정했으며, 수정 뒤 같은 조작 전체에서 앱 응답 유지·충돌 이벤트 0개·설정 임시 파일 0개를 확인했다.

#### Windows 자동 규칙 앱 선택기 검증 (2026-08-09)

- x64 Debug·Release 전체 솔루션 빌드가 경고·오류 없이 통과했고 Activity 21개, Core 13개, Packages 18개, PetLibrary 10개, Settings 37개로 총 99개 xUnit 테스트가 두 구성에서 모두 통과했다.
- Activity 테스트에서 background·tool·owned·cloaked 창과 MonglePet 자체 제외, 식별자 중복 제거·선호·정렬, `.exe` 파일 검사와 누락·잘못된 형식·현재 프로세스 거부를 확인했다.
- 실제 packaged Release에서 MonglePet을 제외한 실행 앱 5개의 이름·아이콘·식별자를 표시하고 Notepad를 선택해 `pfn:microsoft.windowsnotepad_8wekyb3d8bbwe` 규칙으로 저장했다.
- 저장 JSON에는 Notepad 표시 이름이나 실행 파일 전체 경로가 남지 않았고, 앱 응답 유지·충돌 이벤트 0개·설정 임시 파일 0개를 확인했다.
- 자동화 환경에서는 OS 파일 선택 대화상자를 조작할 수 없어 파일 검사 경계와 식별자 저장을 단위 테스트로 먼저 검증했다. 이어 실제 사용자 QA에서 KakaoTalk `.exe` 선택, `exe:kakaotalk.exe` 저장과 전면 앱 전환 시 대상 루틴 실행을 확인했다. Windows Terminal이 호스팅하는 `cmd.exe`는 직접 전면 창을 소유하지 않으므로 Terminal 앱 규칙으로 선택해야 하는 동작도 확인했다.
- QA 뒤 MonglePet·Notepad 프로세스, 임시 패키지 등록과 LocalState를 제거했다.

#### Windows notification area 빠른 제어 검증 (2026-08-09)

- x64 Debug·Release 전체 솔루션 빌드가 경고·오류 없이 통과했고 Activity 21개, Core 13개, Packages 18개, PetLibrary 10개, Settings 37개, Shell 8개로 총 107개 xUnit 테스트가 두 구성에서 모두 통과했다.
- Shell 테스트에서 메뉴 항목 순서·제목·체크·명령, 긴 펫 이름 말줄임과 fallback, 음수 가상 데스크톱을 포함한 작업 영역 우하단 배치와 작은 화면·화면 밖 좌표 clamp를 확인했다.
- 실제 packaged Release에서 `MonglePet · 짱구` 아이콘과 현재 펫, 깨우기·재우기, 클릭 통과, 현재 화면으로 가져오기, 설정과 종료 순서의 네이티브 메뉴를 확인했다.
- 메뉴 명령으로 `awake`/`tuckedAway`와 overlay 표시, `WS_EX_TRANSPARENT`, 현재 화면 식별자·원점 저장을 각각 전환했으며 재실행 뒤 같은 overlay 좌표를 복원했다.
- 설정창 X 닫기 뒤 프로세스·펫·아이콘이 유지되고 아이콘 활성화와 설정 명령으로 같은 창을 다시 열었으며, 명시적 종료 뒤 프로세스와 아이콘이 제거되는 것을 확인했다. 최근 앱 충돌 이벤트와 설정 임시 파일은 없었다.
- `TaskbarCreated` 재등록 코드는 구현했지만 실제 Explorer 재시작과 서로 다른 DPI가 섞인 다중 모니터 장비의 수동 QA는 공개 배포 전 검증으로 남긴다.

#### Windows 이동·드래그·쓰다듬기 검증 (2026-08-09)

- x64 Debug·Release 전체 솔루션 빌드가 경고·오류 없이 통과했고 Activity 21개, Core 27개, Packages 18개, PetLibrary 10개, Settings 37개, Shell 8개로 총 121개 xUnit 테스트가 두 구성에서 모두 통과했다.
- Core 이동 테스트에서 음수 좌표 safe bounds, 마우스 따라가기 목표 거리, 결정적 자유 이동 목표, 도망 목표와 포인터 거리, 속도·경과 시간·정지 반경 advance, Windows 좌상단 좌표의 4·8방향 분류를 확인했다.
- 호버 상태 테스트에서 포인터 주도 진입 뒤 300ms 한 번 실행, 같은 호버 중 중복 억제, 이탈 뒤 재활성화, 정지 포인터 아래로 이동한 펫과 사용자 드래그·비활성 상태 억제를 확인했다.
- 실제 packaged Release에서 마우스 따라가기와 자유 이동의 좌표 변화, 포인터 접근 시 도망가기, `HTCAPTION` 드래그·완료 위치 저장과 재실행 복원을 확인했다. 자동 이동 중에는 저장된 사용자 원점이 바뀌지 않았다.
- `DISPLAY1(-1920, 297)`과 `DISPLAY2(0, 0)` 듀얼 모니터에서 선택 DISPLAY1 자유 이동이 양수 좌표에서 `-1589, 447`까지 진입하고 펫 전체가 작업 영역 안에 유지되는 것을 확인했다.
- 실제 호버에서 `이예~` 쓰다듬기 모션이 한 사이클 재생되고 기존 행동 단계로 복귀했다. 클릭 통과 상태의 HWND는 `HTTRANSPARENT`를 반환하며 호버 감시는 click-through와 독립된 polling 경계를 사용한다.
- 자유 이동 중 오버레이 HWND에 WTS 잠금 메시지를 보내자 좌표 `743, 652`가 1초 이상 유지됐고 해제 메시지 뒤 `44, 23`으로 이동을 재개해 movement timer 생명주기 연결을 확인했다.
- 위치 고정·쓰다듬기 미지정 상태 10초 측정은 전체 시스템 환산 CPU 0.182%, 단일 코어 환산 1.092%, private memory 134.2MiB로 초기 상시 실행 기준을 통과했다.
- QA 설정은 위치 고정·속도 160·모든 화면·이동/쓰다듬기 모션 미지정·클릭 통과 꺼짐과 기존 DISPLAY2 원점으로 복원했다.
- UI Automation이 이동 모드 ComboBox popup을 열린 채 강제 종료하면 `Microsoft.UI.Xaml.dll` 접근 위반이 재현되어, 이동 중 33ms UI 상태 알림을 제거하고 종료 시작 시 Page timer·event를 먼저 분리하며 notification area callback 밖에서 마지막 WinUI 창을 닫도록 생명주기를 보강했다. 자동화도 실제 사용자 선택처럼 popup을 닫도록 교정한 뒤 이동 모드·속도 변경 직후 명시적 종료 3회와 설정 복원 종료 1회에서 새 충돌 이벤트 0개를 확인했다. 설정을 건드리지 않은 명시적 종료도 새 충돌 이벤트 0개였다.

#### Windows 알파 호버와 겹침 투명화 검증 (2026-08-09)

- Core 테스트에서 투명·불투명 알파, 경계 밖 좌표, aspect-fit 여백, RGBA 추출, 좌상단 atlas crop과 최대 64px 축소 영역을 확인했다. 호버 상태는 표시 픽셀 진입 후 300ms 한 번 실행, 투명 픽셀 진입·전환 취소, 패널 이탈 전 중복 억제, 정지 포인터 아래로 이동한 펫과 드래그 억제를 검증했다.
- x64 Debug 빌드는 경고·오류 없이 통과했다. Activity 21개, Core 34개, Packages 18개, PetLibrary 10개, Settings 37개, Shell 8개로 총 128개 xUnit 테스트가 통과했다.
- x64 Release와 `MonglePet.Windows_1.0.0.0_x64.msix` 생성이 성공했다. 로컬 도구 체인에 `mspdbcmf.exe`가 없어 symbols package만 생성되지 않았고 앱 패키지에는 오류가 없었다.
- 실제 packaged Release를 기능이 꺼진 설정으로 시작했을 때 frame player는 `알파 마스크 대기`를 보고했다. 입력 통과와 겹침 투명화를 켜면 현재 모션을 순차 preload해 `알파 마스크 준비 59×64`로 전환했다.
- 포인터를 고정하고 overlay를 같은 frame의 모서리와 중앙 아래로 이동한 QA에서 모서리는 `투명 픽셀`·겹침 없음·기본 100%를 유지했고, 중앙은 `표시 픽셀`·겹침 적용 20%를 보고했다.
- 활성 위치 고정 workload 10초는 전체 시스템 CPU 0.442%, 단일 코어 환산 2.652%, private memory 118.4MiB로 초기 기준을 통과했다. 클릭 통과·겹침 기능·위치·쓰다듬기 설정을 복원해 notification area 명령으로 정상 종료했고 최근 앱 충돌 이벤트는 0건이었다.
- 자동화 환경이 실제 포인터 위치 변경을 거부해 알파 표시 픽셀의 쓰다듬기 진입은 상태 모델 테스트로 검증했다. 물리 포인터와 혼합 DPI 다중 모니터 수동 QA, 실제 WebP alpha fixture는 공개 배포 전 검증으로 남긴다.

#### Windows 전면 앱 대표 창 선호 이동 검증 (2026-08-09)

- Activity resolver 테스트에서 다른 PID, 숨김·owned·tool·cloaked·64px 미만·화면 밖 창을 제외하고 가시 면적이 가장 큰 창을 선택하며, 전체 화면 후보가 있으면 대표 창을 사용하지 않는 것을 확인했다.
- provider 테스트에서 같은 foreground HWND·PID의 1초 캐시, foreground 변경 즉시 갱신, 환경 무효화와 MonglePet 자체 전면 시 창 열거 생략을 확인했다.
- Core Geometry 테스트에서 대표 창과 작업 영역 교집합 안에 펫 전체가 들어가는 목표를 만들고, 창이 펫보다 작으면 기존 화면 무작위 목표로 복구하는 것을 확인했다.
- x64 Debug 빌드는 경고·오류 없이 통과했다. Activity 27개, Core 36개, Packages 18개, PetLibrary 10개, Settings 37개, Shell 8개로 총 136개 xUnit 테스트가 통과했다.
- x64 Release 앱과 MSIX 생성이 성공했다. `mspdbcmf.exe` 부재로 symbols package만 생략됐다.
- 실제 packaged Release에서 사용자의 전체 화면 전면 앱을 `전면 창: 대표 창 없음 또는 전체 화면`으로 판정해 작업 영역 자유 이동으로 복구했다. 선호 토글을 끄면 `전면 창 선호 꺼짐`으로 즉시 전환하고 native 대표 창 조회를 건너뛰었다.
- 자유 이동·전면 창 감지 10초 workload는 전체 시스템 CPU 0.364%, 단일 코어 환산 2.184%, private memory 126.2MiB로 이동 기준을 통과했다. 위치 고정·속도 160·머무름 6초·선호 켜짐·기존 원점으로 복원해 정상 종료했고 최근 충돌 이벤트는 0건이었다.
- 자동화 세션에서는 사용자의 전체 화면 앱이 일반 QA 창의 foreground 전환을 차단했다. 일반 창 내부 목표는 native snapshot resolver와 Geometry 자동 테스트로 검증했으며, 사용자 조작 일반 창·혼합 DPI 다중 모니터 최종 수동 QA는 공개 배포 전 수행한다.

#### Windows 말풍선 런타임과 설정 UI 검증 (2026-08-09)

- Settings 테스트 9개를 추가해 대상 테스트 46개가 통과했다. 행동 대사의 주기 대사 선점, 동일 행동 루틴 중복 억제, 시간 지정 종료 뒤 주기 재예약, 닫기·유지 정책, 순차 주기 목록 순환, 재우기·잠금·절전·펫 전환 timer 정리를 확인했다.
- Windows 좌상단 좌표에서 자동 위 배치와 상단 경계 아래 전환, 음수 좌표 화면, 좌우 오프셋 clamp와 펫을 향하는 꼬리 기준점 계산을 순수 geometry 테스트로 확인했다.
- x64 Debug 앱 빌드는 경고·오류 없이 통과해 별도 non-activating·click-through 말풍선 Win32 HWND, WinUI 3 `DesktopWindowXamlSource`, 행동·활동 생명주기와 대사·정책·테마·배치 설정 UI의 컴파일 연결을 확인했다.
- 전체 Windows 145개 단위 테스트, x64 Release·MSIX와 실제 말풍선의 밝은·어두운 배경, 긴 대사, 혼합 DPI·다중 모니터, 이동 중 위치 추적 및 workload 측정은 남은 구현 완료 뒤 최종 검증에서 수행한다.

### Phase 10 개인 맥 도구 체인 기준선

- 점검일: 2026-07-26
- 저장소: `main`, `origin/main`과 동일한 커밋 `4cc9b0de46cecd0472fdd8711fd7d4a3a77abf25`
- 도구 체인: Xcode 26.6(17F113), Swift 6.3.3, macOS 26.3.1
- 순수 도메인 테스트는 통과했지만 `MainActor` 객체를 해제하는 XCTest에서 시스템 `libswift_Concurrency`의 `swift::TaskLocal::StopLookupScope` 잘못된 해제가 반복 재현됐다.
- Address Sanitizer 호출 스택이 알려진 Swift isolated-deinit/task-local 런타임 결함과 일치함을 확인했다. 제품 코드와 테스트에 임시 빈 소멸자 또는 대규모 `Task.detached` 우회를 남기지 않았다.
- `softwareupdate --list`에서 macOS Tahoe 26.5.2가 확인됐다. 시스템 업데이트와 재시작 후 전체 `MonglePetTests`를 직렬 재실행하고, 통과 전에는 Phase 10 Release·Preview 기준선으로 인정하지 않는다.
- macOS 26.5.2(25F84) 업데이트 후 같은 Xcode 26.6 환경에서 전체 단위 XCTest 308개가 실행됐고 선택적 로컬 WebP fixture 1개만 건너뛴 채 실패 없이 통과했다. 이전 종료 오류가 재현되지 않아 개인 맥 Release·Preview 자동 검증 기준선으로 인정한다.
- 2026-07-27 회사 맥의 별도 원격 작업 트리에서 전체 단위 XCTest 307개 통과·선택 fixture 1개 건너뜀, Universal Preview ZIP 생성과 SHA-256·manifest 재검증을 완료했다. Preview 스크립트는 dirty 작업 트리와 이전 `ALLOW_DIRTY=1` 지정 모두에서 빌드 전에 실패하도록 확인했다.

### Phase 1 기준선

- 측정일: 2026-07-21
- 대상: Debug 빌드, 정적 펫 표시 및 사용자 입력이 없는 상태
- 방법: `top`으로 1초 간격 5회 측정
- 결과: CPU 5회 모두 0.0%, 메모리 약 26MB
- 비고: 애니메이션 런타임이 추가되는 Phase 2에서 Instruments로 다시 측정한다.

### Phase 2B 패키지 로더 검증

- 측정일: 2026-07-21
- 실제 PNG 및 정적 WebP fixture로 이미지 형식, 픽셀 크기와 알파 판별 확인
- 경로 탈출, 심볼릭 링크, 스크립트, 누락·중복 참조와 자원 상한 거부 확인
- 패키지 로더 테스트 15개 및 전체 단위 테스트 38개 통과
- 알파 포함 lossless/lossy WebP와 Codex atlas fixture는 Phase 2D에서 추가 검증

### Phase 2C 패키지 설치 검증

- 측정일: 2026-07-21
- 실제 ZIP 생성 후 security-scoped 접근, 임시 추출, 재검증과 UUID 라이브러리 설치 확인
- 경로 탈출, 절대 경로, 심볼릭 링크, 경로 충돌, 손상 ZIP과 압축 폭탄 제한 확인
- 중복 거부, 별도 사본, 같은 패키지 ID 교체와 실패 시 workspace·staging 정리 확인
- 패키지 설치 테스트 10개 및 전체 단위 테스트 48개 통과

### Phase 2D 호환 가져오기 검증

- 측정일: 2026-07-21
- 생성한 1536×1872 v1 및 1536×2288 v2 atlas로 Codex 행, 시간과 16방향 매핑 확인
- 비어 있는 사용 셀, 불투명한 미사용 셀, version/크기 불일치와 경로 탈출 거부 확인
- 실제 로컬 Codex legacy WebP 펫을 저장소에 복사하지 않고 변환 및 재검증 성공
- ImageIO로 만든 GIF/APNG의 프레임 시간, 투명 GIF canvas 합성과 100ms 지연 시간 폴백 확인
- PNG 시퀀스가 호출자 지정 순서를 보존하고 모든 결과가 기존 패키지 로더를 통과하는지 확인
- 전체 단위 테스트 62개 통과, 환경 변수로 실행하는 로컬 WebP 수동 fixture 테스트 1개는 기본 실행에서 skip

### Phase 3 행동 엔진 검증

- 측정일: 2026-07-22
- `tuckedAway`, 잠금·절전, 상호작용, 수동, 장시간 유휴, 앱 규칙, 짧은 유휴와 기본 행동의 우선순위 확인
- 2분·10분 진입 경계와 입력 재개 후 3초 직전·정확한 이탈 경계 확인(당시 정책이며 D-055에서 즉시 해제로 대체)
- 높은 `priority` 우선과 같은 값의 안정적인 배열 순서 확인
- 반복·비반복 목록, 같은 목록 진행 보존, 다른 목록의 단계 경계 전환과 pause·resume 확인
- 쓰다듬기 입력 합치기, 기존 단계의 남은 시간 복귀, 500ms cooldown 확인
- 누락 모션의 `idle` 폴백과 `idle`도 없는 펫의 실행 불가 상태 확인
- 행동 엔진 테스트 14개 및 전체 단위 테스트 76개 통과, Debug 빌드 성공

### Phase 4 macOS 활동 감지 검증

- 측정일: 2026-07-22
- 격리된 NotificationCenter로 전면 앱 bundle identifier 변경과 세션·화면·전원 상태 변환 확인
- 가짜 clock, idle provider와 poller로 초기 snapshot, 1초 갱신과 앱 변경 즉시 발행 확인
- 사용자 세션 비활성화·화면 수면·시스템 절전 중 polling 중지 및 복귀 후 유휴 시간 재조회 확인
- 실제 `CGEventSource` 유휴 시간 조회가 별도 권한 설정 없이 테스트 호스트에서 동작하는지 확인
- 시스템 일시 중지가 사용자의 awake/tuckedAway 상태를 바꾸지 않고 애니메이션만 중지하는지 확인
- 활동 감지 관련 테스트 8개 및 전체 단위 테스트 84개 통과, Debug 빌드 성공
- 실제 앱에서 별도 권한 요청 없이 잠금·해제 및 절전·깨우기 후 애니메이션 재개 확인
- 사용자 재우기 상태에서 잠금·해제 후 계속 숨김을 유지하고 직접 깨울 때만 재개하는지 확인

### Phase 5A 설정 저장과 복원 기반 검증

- 측정일: 2026-07-22
- 기본값 로드와 행동 목록·자동 규칙·overlay를 포함한 schema-v1 저장 후 재로드 결과 확인
- 정수 밀리초, 명시적 enum 문자열과 `application`·`idleAtLeast` 조건 discriminator 확인
- 잘못된 필드·UUID·목록 참조의 항목 단위 복구와 알 수 없는 조건 type 보존·비활성화 확인
- 손상·5MiB 초과 파일 격리, 미래 스키마 원본 보존과 쓰기 차단 확인
- 기존 파일 원자적 교체, 실패 시 임시 파일 정리와 유효하지 않은 Domain 설정 저장 거부 확인
- 설정 저장 테스트 7개와 미지원 자동 규칙 무시 테스트 1개 추가, 전체 단위 테스트 92개 통과, Debug 빌드 성공
- 이 단계는 앱 런타임과 설정 UI에 연결하지 않았으므로 별도 수동 QA 대상은 없음

### Phase 5B 설정 적용과 기본 설정 UI 검증

- 측정일: 2026-07-22
- 설정 세션의 즉시 변경, 명시적 저장, 오류 안내와 미래 스키마 원본 보존 확인
- 첫 실행 우하단 배치, 저장 위치 복원, 화면 밖 보정과 적용 좌표의 메모리 동기화 확인
- 192:208 종횡비 크기 변경과 `NSPanel.ignoresMouseEvents` 클릭 통과 적용 확인
- 저장된 `awake`·`tuckedAway`, 행동 모드, 크기, 클릭 통과와 위치의 재실행 복원 확인
- 전체 단위 테스트 99개 통과, 환경 변수 기반 로컬 WebP fixture 테스트 1개는 기본 실행에서 skip, Debug 빌드 성공
- 실제 앱에서 설정창 제어, 크기 즉시 반영, 클릭 통과, 드래그와 완전 종료 후 전체 설정 복원 수동 QA 통과

### Phase 5C 행동 런타임과 기본 프리셋 검증

- 측정일: 2026-07-22
- 내장 idle·focus·rest·sleep 모션의 atlas 범위와 프리셋 motion ID 일치 확인
- 가짜 clock과 일회성 scheduler로 수동 목록의 3초 단계 경계 전환과 자동 2분·10분 유휴 규칙 확인
- 재우기·잠금·절전 중 timer 취소, 남은 단계 시간 보존과 복귀 후 재예약 확인
- `playbackSpeed`에 따른 프레임 delay와 잘못된 속도의 1.0배 복구 확인
- 전체 단위 테스트 105개 통과, 환경 변수 기반 로컬 WebP fixture 테스트 1개는 기본 실행에서 skip, Debug 빌드 성공
- 실제 앱에서 수동 4개 프리셋 전환, 수동 선택·모드 재실행 복원과 잠금 해제 후 애니메이션 재개 확인
- 실제 2분 유휴 대기 QA는 생략했으며 같은 경계는 가짜 시계 단위 테스트로 검증

### 사용자 자유 행동 기본값 전환 검증

- 측정일: 2026-07-22
- 첫 실행 설정을 단일 `기본` 루틴과 자동 규칙 0개로 정규화하는지 확인
- 기본 루틴과 새 사용자 루틴이 예약된 현재 펫 기본 애니메이션 참조로 시작하는지 확인
- 앱·유휴 조건이 동시에 일치할 때 조건 종류가 아닌 큰 `priority`와 안정적인 배열 순서로 평가하는지 확인
- `idle` 애니메이션이 없는 펫에서도 선언된 기본 애니메이션을 재생하고 정상 참조를 오류 폴백으로 표시하지 않는지 확인
- 정확히 일치하는 구형 4개 루틴·2개 규칙은 새 기본값으로 전환하고 수정된 구형 데이터는 보존하는지 확인
- 전체 XCTest 136개 통과, 선택적 로컬 WebP fixture 1개 건너뜀
- 단일 기본 루틴 편집, 사용자 루틴·자동 규칙 구성과 재실행 복원 실제 앱 QA 통과

### Phase 6 펫 상세와 애니메이션 미리보기 검증

- 측정일: 2026-07-22
- overlay와 설정 미리보기가 내장·설치 펫에 동일한 atlas 로더를 사용하는지 확인
- 설정 미리보기에서는 창 드래그를 비활성화하고 선택 애니메이션의 실제 프레임 재생을 기존 `FramePlayer`로 연결
- 현재 펫의 전체 애니메이션 목록, 기본 표시, 프레임 수·사이클 시간·반복 요약을 설정 UI에 제공
- 전체 단위 XCTest 137개 통과, 선택적 로컬 WebP fixture 1개 건너뜀
- 설정창에서 미리보기와 애니메이션 목록 노출을 확인하는 대상 XCUITest 1개 통과
- 실제 앱에서 애니메이션 선택 전환, 표시 정보와 펫 교체 시 기본 애니메이션 복귀 QA 통과

### Phase 6 사용자 펫 메타데이터 편집 검증

- 측정일: 2026-07-22
- 새 사용자 펫의 이름·제작자·버전·설명을 manifest에 정규화해 저장하고 `license`를 기록하지 않는지 확인
- 기존 사용자 펫의 메타데이터와 기본 애니메이션을 임시 사본에서 수정하고 같은 설치 UUID로 원자적 교체하는지 확인
- 패키지 ID, 애니메이션과 편집 marker가 유지되고 저장 후 라이브러리 선택과 런타임에 변경 정의를 다시 전달하는지 확인
- 존재하지 않는 기본 애니메이션과 읽기 전용 가져온 펫의 수정을 거부하고 기존 manifest를 변경하지 않는지 확인
- 전체 단위 XCTest 141개 통과, 선택적 로컬 WebP fixture 1개 건너뜀
- 설정창 기본 스모크와 새 펫 메타데이터 입력란을 확인하는 관련 XCUITest 2개 통과
- 실제 앱에서 새 펫 메타데이터 입력, 정보 수정, 기본 애니메이션 전환, 즉시 반영, 재실행 복원과 읽기 전용 UI QA 통과

### Phase 6 사용자 펫 애니메이션 수정·삭제 검증

- 측정일: 2026-07-22
- 애니메이션 이름과 반복 설정을 수정하면서 프레임·atlas·패키지 ID와 설치 UUID를 유지하는지 확인
- 기본 애니메이션 이름 변경 시 manifest 기본 ID와 행동 단계 참조가 함께 변경되는지 확인
- 비기본 애니메이션 삭제 시 더 이상 사용하지 않는 atlas 파일과 manifest 항목만 제거하는지 확인
- 기본 애니메이션과 마지막 남은 애니메이션 삭제를 패키지 변경 전에 차단하는지 확인
- 당시 schema-v1에서 삭제된 애니메이션을 참조하던 행동 단계의 시간·속도는 유지하고 현재 펫 기본 애니메이션 참조로 복구·저장하는지 확인
- 전체 단위 XCTest 146개 통과, 선택적 로컬 WebP fixture 1개 건너뜀
- 설정창 대상 XCUITest 1개 통과
- 실제 앱에서 이름·반복 수정, 기본·마지막 삭제 보호, 비기본 삭제, 참조 복구와 재실행 복원 QA 통과

### Phase 6 읽기 전용 펫의 편집 가능한 사본 검증

- 측정일: 2026-07-22
- 읽기 전용 설치의 자산과 메타데이터를 임시 사본에 복제하고 새 패키지 ID와 편집 marker를 기록한 뒤 전체 검증하는지 확인
- 사본이 새 설치 UUID로 추가되고 생성 직후 선택되어 라이브러리와 런타임에 전달되는지 확인
- 원본 설치, 패키지 ID와 manifest가 사본 생성 및 이후 사본 편집에도 변경되지 않는지 확인
- 이미 편집 가능한 사용자 펫의 중복 사본 생성 API 호출을 거부하고 내장 펫에는 사본 버튼을 노출하지 않는지 확인
- 전체 단위 XCTest 148개 통과, 선택적 로컬 WebP fixture 1개 건너뜀
- 설정창 대상 XCUITest 1개 통과
- 실제 앱에서 읽기 전용 샘플 펫 가져오기, 사본 생성, 원본 보존과 편집 가능 상태 전환 QA 통과

### Phase 7 schema-v2 마이그레이션 계약 검증

- 측정일: 2026-07-22
- schema-v1 입력과 schema-v2 기대 JSON fixture를 디코딩해 선택된 설치 UUID의 첫 행동 프로필로 변환하는지 확인
- 기존 유지 시간을 선택 펫 패키지의 프레임 사이클로 나눠 가장 가까운 1 이상의 반복 횟수로 변환하는지 확인
- 단계별 재생 속도는 변환하지 않고, 없는 애니메이션은 현재 펫 기본 애니메이션 참조와 반복 1회로 복구하는지 확인
- 내장 펫과 설치 UUID 키가 명시적 discriminator로 왕복되고 알 수 없는 키 종류를 거부하는지 확인
- v1 이외의 직접 마이그레이션 요청을 거부하고 schema-v2 DTO 계약과 기존 schema-v1 저장·세션 회귀 테스트가 유지되는지 확인
- 전체 단위 XCTest 152개 통과, 선택적 로컬 WebP fixture 1개 건너뜀
- 이 계약 단계에서는 실제 앱 저장 형식을 후속 Domain·런타임 전환이 끝날 때까지 schema-v1로 유지

### Phase 7 프레임별 사용자 애니메이션 편집 검증

- 기존 프레임의 일부 유지·삭제·순서 변경과 새 PNG 추가 결과를 새 전용 atlas와 manifest로 재구성하는지 확인
- 프레임별 16~60000ms 경계, 빈 애니메이션 거부와 실패 시 설치 manifest 불변성을 확인
- 8열을 넘는 atlas의 두 번째 행 프레임도 manifest의 좌상단 좌표대로 보존하는지 실제 픽셀로 확인
- 수정 대상 atlas가 더 이상 참조되지 않을 때만 파일을 제거하고 같은 설치 UUID로 원자적 교체하는지 확인
- 투명 영역을 잘라 지정한 공통 캔버스의 좌상단 좌표와 배율로 합성하고, 저장된 atlas의 가시 영역이 편집 미리보기와 일치하는지 확인
- 새 펫·애니메이션 등록 화면이 제한된 높이에서 내용만 스크롤하고 PNG 선택과 저장 버튼을 함께 접근할 수 있는지 UI 테스트로 확인
- 첫 선택 이후 `PNG 추가`가 기존 초안을 보존하는지, 선택 프레임 고정 보기와 전체 재생·일시정지, 프레임별 간격·배치 편집이 독립적으로 동작하는지 확인
- Debug 빌드 성공, 전체 단위 XCTest 155개 통과, 선택적 로컬 WebP fixture 1개 건너뜀, 기존 XCUITest 5개 통과
- 실제 앱의 선택·전체 미리보기, 일시정지, 시간 입력, 순서 변경, PNG 누적 추가·삭제, 프레임별 배율·위치 보정과 저장 후 재생은 수동 QA 대기

### Phase 7 사이클 행동·펫별 schema-v2 설정 검증

- 프레임 시간 합계로 계산한 전체 애니메이션 사이클과 단계별 `repeatCount`, 동일 행동의 진행 위치 유지, 다른 행동·편집 행동의 즉시 전환과 잠금·복귀 후 남은 사이클 시간 보존을 확인
- 내장 펫 예약 키와 설치 UUID별 프로필의 모드·수동 선택·루틴·자동 규칙이 펫 전환과 재실행 뒤 서로 섞이지 않고 복원되는지 확인
- 다중 프로필의 순서 보존 저장 왕복, 잘못된 키와 중복 키의 프로필 단위 복구, v2 단계 유효성 검사를 확인
- schema-v1 유지 시간을 선택 펫의 실제 사이클 반복 횟수로 변환하고 성공한 v2 결과만 원자적으로 교체하는지 확인
- 마이그레이션에 필요한 펫 정의를 얻지 못하면 v1 원본을 변경하지 않고 설정 쓰기를 차단하며, 이후 정의를 제공하면 정상 변환하는지 확인
- Debug 빌드 성공, 전체 단위 XCTest 162개 통과, 선택적 로컬 WebP fixture 1개 건너뜀, 기존 XCUITest 5개 통과
- 실제 앱에서 행동 단계의 반복 횟수 편집, 두 펫 사이의 행동 설정 격리와 왕복 복원, 완전 종료 후 재실행 복원 QA 통과

### Phase 8 로컬 펫 공유 검증

- 설치 패키지를 다시 검증하고 현재 schema manifest와 참조 이미지 자산만 선별해 ZIP 기반 `.monglepet`로 원자적 저장하는지 확인
- 편집 marker, 사용하지 않는 파일과 알 수 없는 manifest 필드가 제외되고 완성 아카이브의 추출·재로드 결과가 원본 메타데이터와 애니메이션 정의에 일치하는지 확인
- 20 MiB 제한, 잘못된 목적지, 원본 변경·재검증 실패에서 내보내기를 거부하고 기존 목적지 파일을 보존하는지 확인
- 기존 `license` 유무나 문자열과 관계없이 사용자 게시 권한 확인을 요구하고, 검토 후 이름·버전·제작자 변경 시 재확인하는지 확인
- 내장 펫 공유 제외, 선택 설치 펫의 검토·내보내기 전달, 진행 상태와 오류 표시를 확인
- 가져올 메타데이터와 같은 패키지 ID의 모든 설치 후보를 비교하고 사용자가 새 설치 또는 명시적 설치 UUID 교체를 선택하는지 확인
- 공유 권한 확인부터 내보낸 아카이브를 새 라이브러리에 새 설치 UUID로 가져오는 전체 왕복에서 메타데이터·애니메이션·이미지 일치와 읽기 전용 상태를 확인
- marker, 설치 UUID, 행동 프로필과 자동 규칙이 공유 manifest와 파일 목록에 포함되지 않으며 공유·가져오기가 원본 사용자 펫을 변경하지 않는지 확인
- 기존 설치 테스트로 경로 탈출, 절대 경로, 심볼릭 링크, 경로 충돌, 손상 ZIP, 크기·엔트리·압축률 제한과 실패 후 임시 작업 공간 정리를 확인
- Debug 빌드 성공, 전체 단위 XCTest 178개 통과, 선택적 로컬 WebP fixture 1개 건너뜀
- 실제 앱에서 권한 확인, 저장 위치 선택, `.monglepet` 저장·재가져오기, 새 설치, 다중 중복 후보 선택과 기존 항목 교체 QA 통과

### Phase 9-1 펫별 이동 설정과 schema-v3 검증

- `fixed`, `cursorFollowing`, `freeRoaming` 이동 모드와 속도·거리·정지 반경·자유 이동 대기 시간·전면 창 선호 설정의 저장 왕복 확인
- 내장 펫과 설치 UUID별 이동 설정이 펫 전환과 재실행 뒤 서로 섞이지 않고 복원되는지 확인
- 행동 모드와 루틴을 편집해도 같은 펫의 이동 설정이 유지되는지 확인
- 잘못된 이동 enum과 범위 밖 값을 필드별 기본값으로 복구하고 유효하지 않은 Domain 저장을 거부하는지 확인
- schema-v2의 각 펫 프로필에 기본 위치 고정 설정을 추가하고 schema-v3로 원자적 교체하는지 확인
- schema-v1을 v2 행동 프로필과 v3 이동 설정으로 순차 변환하며 펫 정의가 없을 때 원본을 보존하는지 확인
- Debug 빌드 성공, 전체 단위 XCTest 185개 통과, 선택적 로컬 WebP fixture 1개 건너뜀

### Phase 9A 공유 권장 프로필 검증

- 내보낸 선택 프로필의 encode/decode와 패키지 모션 참조 무결성을 확인
- 가져오기 전 펫 정보와 행동·이동 요약을 표시하고 `펫만 설치`와 `권장 설정 적용 후 설치`를 분리하는지 확인
- 권장 설정 적용 시 새 설치 UUID의 로컬 프로필로 변환·저장되고, 펫만 설치 시 독립 기본 프로필을 사용하는지 확인
- 같은 패키지의 새 설치에서도 사용자가 선택한 적용 여부를 유지하고 기존 설치 교체에는 기본적으로 적용하지 않는지 확인
- 기존 설치 교체의 기본 선택이 로컬 프로필을 보존하고, 명시적 전체 교체가 선택한 설치 UUID의 프로필만 바꾸며 다른 펫과 overlay 설정을 유지하는지 확인
- 미래 schema와 의미상 손상된 권장 설정은 경고 후 펫만 설치하며 1 MiB 초과는 전체 패키지를 거부하는지 확인
- 검토 이후 패키지 메타데이터·애니메이션·권장 설정이 달라지면 설치 전 다시 확인하도록 거부하는지 확인
- 3단계 자동 검증에서 전체 단위 XCTest 255개 통과, 선택적 로컬 WebP fixture 1개 건너뜀, Debug 빌드 성공
- 4단계 자동 검증에서 전체 단위 XCTest 257개 통과, 선택적 로컬 WebP fixture 1개 건너뜀, Debug 빌드 성공
- 5단계에서 권장 프로필과 앱 규칙을 포함해 내보낸 아카이브를 빈 라이브러리에 새 UUID로 설치하고, 권장 설정 적용·설정 파일 재로드까지 종단 간 왕복하는지 확인
- 권장 프로필이 없는 기존 패키지는 이전과 동일하게 설치되고, 완전히 손상된 선택 프로필과 미래 schema는 펫 설치를 막지 않으며 권장 설정만 적용하지 않는지 확인
- 5단계 자동 검증에서 전체 단위 XCTest 260개 통과, 선택적 로컬 WebP fixture 1개 건너뜀, Debug 빌드 성공
- 실제 앱에서 앱 규칙을 포함한 내보내기, 새 설치의 권장 설정 적용, 행동·이동·쓰다듬기·앱 규칙 복원, 완전 종료 후 재실행 복원과 기존 패키지 설치 수동 QA 통과

### Phase 9B 계획 검증

- 좌·우·위·아래, 높이가 다른 화면, 음수 좌표와 visible frame 사이 빈 간격에서 화면 연결 경로와 정체 복구 확인
- 낮은 속도와 작은 tick에서도 마우스 따라가기가 대상 화면으로 진입하고 이동 애니메이션은 좌표 경로와 독립적으로 유지되는지 확인
- 전체 디스플레이·선택 모니터·정규화 사용자 지정 영역에서 마우스 따라가기와 자유 이동 목표가 범위를 벗어나지 않는지 확인
- 디스플레이 분리·재연결과 visible frame 변경 시 저장한 영역의 안전 폴백·복원 확인
- schema-v3 → v4 기본값 마이그레이션, v4 저장 왕복, 잘못된 투명도·영역 복구와 미래 스키마 보호 확인
- 클릭 통과·awake·화면 사용 가능 조건, 현재 프레임 알파 영역 겹침, 투명도 전환과 비활성 timer 해제 확인
- 앱 SemVer 정수 비교, 최소 버전 미충족 차단, 더 새로운 제작 버전 경고와 호환 정보 없는 기존 패키지 허용 확인
- 앱 버전·빌드 표시와 호환 필드 포함 `.monglepet` 내보내기·가져오기 왕복 확인
- 실제 다중 모니터, 클릭 통과, 완전 종료·재실행과 기존 설정·기존 패키지 수동 QA

### Phase 9B 1단계 다중 모니터 경로 자동 검증

- 측정일: 2026-07-23
- 좌우·상하 화면 전환, 높이가 다른 화면과 화면 사이 빈 간격에서 현재 화면 출구·대상 화면 진입점 계산 확인
- 같은 화면에서는 전환 경로를 만들지 않고 기존 마우스 목표 계산을 유지하는지 확인
- `20pt/s`의 낮은 속도와 1pt 미만 tick 이동을 보존하고 이동 애니메이션 상태를 유지하는지 확인
- 대상 화면 진입 요청이 적용되지 않는 가짜 panel adapter에서 안전한 최종 목표로 복구하는지 확인
- 전체 단위 XCTest 267개 통과, 선택적 로컬 WebP fixture 1개 건너뜀, Debug 빌드 성공
- 실제 다중 모니터 환경에서 낮은 속도의 화면 왕복과 이동 애니메이션 수동 QA 통과

### Phase 9B 2단계 이동 범위와 schema-v4 자동 검증

- 측정일: 2026-07-23
- 전체 화면·선택 모니터·정규화 사용자 영역을 실제 원점 범위로 변환하고 마우스 따라가기·자유 이동 목표를 제한하는지 확인
- 저장된 디스플레이 UUID를 찾지 못하면 선택 정보는 유지하고 실행 중 전체 화면으로 폴백하는지 확인
- 이동 범위 변경이 펫별 이동 프로필을 바꾸지 않고 즉시 저장되며 재실행 뒤 복원되는지 확인
- schema-v3의 overlay·모든 펫 프로필을 유지하면서 이동 범위와 투명도 기본값을 추가해 v4로 원자적 교체하는지 확인
- schema-v1·v2 순차 마이그레이션, schema-v4 저장 왕복, 잘못된 사용자 영역 복구와 미래 schema 쓰기 차단 확인
- 범위를 다른 모니터로 바꾼 상태에서 마우스 따라가기와 자유 이동이 저속에서도 실제 화면 출구·진입 경로를 사용하는지 확인
- 전체 단위 XCTest 282개 통과, 선택적 로컬 WebP fixture 1개 건너뜀, Debug 빌드 성공
- 실제 앱에서 세 범위 모드의 마우스 따라가기·자유 이동·위치 고정과 완전 종료 후 복원 QA 통과

### Phase 9B 3단계 투명도와 알파 겹침 자동 검증

- 측정일: 2026-07-23
- 기본 투명도와 겹침 투명도 설정의 허용 범위 보정, 즉시 적용과 schema-v4 저장·복원 확인
- 현재 atlas의 좌상단 원점 source rect를 실제 표시 방향과 일치시켜 프레임 안의 투명·불투명 픽셀을 구분하는지 확인
- 표시 영역의 종횡비 여백을 겹침에서 제외하고 겹침 투명도가 기본 투명도보다 높을 때 불투명도가 증가하지 않는지 확인
- 클릭 통과, awake, 화면 사용 가능, 동작 줄이기 꺼짐 조건을 모두 만족할 때만 100ms 일회성 timer를 이어가는지 확인
- 기능 비활성화, 재우기, 잠금·절전과 동작 줄이기 전환에서 timer를 해제하고 기본 투명도로 복귀하는지 확인
- 프레임별 최대 64×64 알파 마스크를 최대 256개까지 캐시해 반복 재생 중 atlas를 다시 분석하지 않는 구조 확인
- 전체 단위 XCTest 288개 통과, 선택적 로컬 WebP fixture 1개 건너뜀, Debug 빌드 성공
- 실제 앱에서 기본 투명도, 겹침 진입·해제, 투명 여백 제외와 완전 종료 후 설정 복원 수동 QA 통과

### Phase 9B 4단계 앱·패키지 호환 버전 자동 검증

- 측정일: 2026-07-23
- 앱 target의 표시 버전 `0.1.0`, 빌드 번호 `1`과 설정 화면의 `MonglePet 0.1.0 (1)` 표시 확인
- `MAJOR.MINOR.PATCH` 정수 구성 요소 비교와 누락·부분 호환 정보, 잘못된 문자열·선행 0·정수 범위 초과 거부 확인
- 최소 앱 버전 미충족 패키지는 검토 내용을 제공하되 직접·검토 설치를 차단하고, 제작 버전만 더 새로운 패키지는 경고 상태로 설치 허용
- 호환 정보가 없는 기존 패키지는 계속 설치하고 지원하지 않는 선택 권장 프로필의 기존 정책을 유지하는지 확인
- 사용자 펫 생성·편집, GIF·APNG·PNG·Codex 변환과 내보내기에서 현재 앱 제작·최소 버전을 자동 기록하는지 확인
- 호환 필드를 포함한 내보내기 아카이브의 압축 해제·재검증·새 라이브러리 설치 왕복 확인
- 전체 단위 XCTest 297개 통과, 선택적 로컬 WebP fixture 1개 건너뜀, Debug 빌드 성공
- 설정 화면 앱 버전 표시를 포함한 대상 XCUITest 1개 통과
- 실제 앱에서 `MonglePet 0.1.0 (1)` 표시, 새 내보내기 호환 정보, 호환 정보 없는 기존 패키지와 가져오기 검토 화면 수동 QA 통과

### Phase 9B 5단계 전체 회귀 검증

- 측정일: 2026-07-23
- 전체 단위 XCTest 297개 통과, 선택적 로컬 WebP fixture 1개 건너뜀
- 설정·이동·펫 생성·overlay와 Light·Dark 실행을 포함한 전체 XCUITest 6회 통과
- SwiftUI `TabView`가 macOS 접근성 계층에서 이동 탭을 `RadioButton`으로 노출하는 실제 역할에 맞춰 UI 테스트 조회를 수정
- Debug 빌드 성공
- 실제 앱에서 다중 모니터 이동, 세 이동 범위, 기본·겹침 투명도와 재실행 복원 수동 QA 통과
- 현재 설정·펫·행동 루틴 보존, 호환 정보가 없는 기존 패키지와 호환 정보가 포함된 새 패키지 가져오기 수동 QA 통과

### Phase 9-2 순수 이동 좌표 모델 검증

- AppKit 없이 `Double` 기반 좌하단 원점 좌표로 펫 전체가 화면 안전 영역에 들어오는 원점 범위와 큰 펫의 중앙 폴백 확인
- 커서와 설정 거리 유지, 화면 가장자리 보정, 화면 밖 포인터의 가장 가까운 화면 선택과 음수 좌표 보조 모니터 확인
- 자유 이동의 결정적 화면 선택, 대표 창 안 목표 선호와 창이 너무 작을 때 화면 안전 영역 폴백 확인
- 초당 이동 속도, 경과 시간과 정지 반경에 따른 다음 원점·실제 이동·도착 상태 확인
- 이동 좌표 테스트 12개 및 전체 단위 XCTest 197개 통과, 선택적 로컬 WebP fixture 1개 건너뜀, Debug 빌드 성공

### Phase 9-3 전면 앱 대표 창 provider 검증

- Core Graphics 좌상단 창 좌표를 AppKit과 이동 모델의 좌하단 좌표로 변환하고 위·아래 보조 모니터의 음수 좌표 확인
- 전면 앱 PID가 아닌 창, 일반 layer가 아닌 창, 완전 투명·너무 작은 창과 알려진 화면 밖 창 제외 확인
- 같은 앱의 여러 창 중 화면 가시 면적이 가장 큰 창을 선택하고 전체 화면에 가까운 창은 `nil`로 복구하는지 확인
- 동일 PID는 기본 갱신 간격 동안 Window Server 결과와 `nil`을 캐시하고, PID 변경·명시적 무효화 시 즉시 다시 조회하는지 확인
- 실제 시스템 provider가 별도 접근성 권한 설정이나 요청 없이 조회를 완료하는지 확인
- 대표 창 provider 테스트 11개 및 전체 단위 XCTest 208개 통과, 선택적 로컬 WebP fixture 1개 건너뜀, Debug 빌드 성공

### Phase 9-4 이동 controller와 scheduler 검증

- 위치 고정 또는 이동 불허 상태에서는 timer와 포인터·창 조회를 시작하지 않고 중지 시 예약 작업을 남기지 않는지 확인
- 마우스 따라가기에서 설정 속도로 이동하고 실제 좌표 변경이 있을 때만 모드별 애니메이션 ID와 이동 상태를 보고하는지 확인
- 이동 애니메이션이 미지정이어도 실제 이동 상태는 유지하고, 정지 히스테리시스 뒤 정지로 전환하는지 확인
- 자유 이동이 대표 창 또는 화면 목표로 이동한 뒤 150ms 정착 구간과 설정된 대기 시간의 일회성 timer를 거쳐 새 목표를 만드는지 확인
- 이동 중 약 33ms, 커서 정지 시 100ms, 화면 환경 누락 시 1초 재시도 간격과 모드 변경·화면 무효화 재시작 확인
- 이동 controller 테스트 11개 및 전체 단위 XCTest 219개 통과, 선택적 로컬 WebP fixture 1개 건너뜀, Debug 빌드 성공

### Phase 9-5 Overlay 이동 생명주기 검증

- 패널 원점·크기 adapter와 자동 이동 원점 적용이 사용자 위치 저장 callback을 발생시키지 않는지 확인
- 사용자 드래그 시작 시 자동 이동을 먼저 중지하고 종료 후 사용자 위치 저장과 자동 이동 재개 순서를 확인
- 깨움과 활성 시스템 상태가 모두 충족될 때만 이동을 허용하고 재우기·잠금·절전·동작 줄이기가 각각 즉시 이동을 막는지 확인
- 디스플레이 구성과 펫 변경 시 이동 환경을 무효화하고, 자동 이동 중 좌표와 화면 보정 좌표를 설정에 저장하지 않는 경계를 확인
- 생명주기·Overlay·`AppCoordinator` 통합 테스트 6개 추가, 전체 단위 XCTest 225개 통과, 선택적 로컬 WebP fixture 1개 건너뜀, Debug 빌드 성공

### Phase 9-6 펫별 이동 설정 UI 검증

- 별도 이동 탭에서 현재 설정 대상 펫과 위치 고정·마우스 따라가기·자유 이동 세 모드를 표시하는지 확인
- 위치 고정에는 드래그 안내만, 마우스 따라가기에는 속도·정지 반경·마우스 거리, 자유 이동에는 속도·정지 반경·머무는 시간·전면 창 선호만 노출하는지 확인
- 모드·전면 창 선호는 즉시 저장하고 슬라이더는 런타임에 즉시 반영하되 조작 종료 전에는 설정 파일을 쓰지 않는지 확인
- 펫 전환과 재실행 뒤 각 펫의 이동 설정이 독립적으로 복원되는 기존 schema-v3 테스트를 함께 실행
- 이동 탭 대상 XCUITest를 추가하고 테스트 타깃 컴파일 확인. 전체 단위 XCTest 226개 통과, 선택적 로컬 WebP fixture 1개 건너뜀
- 실제 앱에서 세 모드 전환, 모드별 설정 즉시 반영, 펫별 설정 전환과 재실행 복원 수동 QA 통과

### Phase 9-7 이동 애니메이션과 쓰다듬기 검증

- 실제 자동 이동 중에만 모드별 선택 애니메이션을 행동 위에 표시하고, 도착하면 최신 행동 재생으로 돌아가는지 확인
- 이동 애니메이션이 미지정·누락이면 행동 표시를 유지하고, 이동 중 행동이 바뀌어도 이동 표시를 불필요하게 재시작하지 않는지 확인
- 쓰다듬기가 이동 표시보다 우선하고 완료 뒤 중단한 행동 단계의 남은 시간으로 복귀하는지 확인
- 연속 쓰다듬기 입력 합치기와 500ms cooldown, 클릭·드래그 구분, 드래그 위치 저장 경계를 확인
- 이동·쓰다듬기 애니메이션의 펫별 저장, 이전 schema-v3 호환, 이름 변경·삭제 참조 복구를 확인
- 전체 단위 XCTest 234개 통과, 선택적 로컬 WebP fixture 1개 건너뜀, Debug 빌드 성공
- 이동 탭 XCUITest에 쓰다듬기 선택 항목 확인을 추가하고 테스트 타깃 컴파일 확인
- 실제 앱에서 이동 중 선택 애니메이션, 도착 후 행동 복귀, 쓰다듬기 일회성 반응과 드래그 구분 수동 QA 통과

### Phase 10E 로그인 시 자동 실행 설정 검증

- 측정일: 2026-07-24
- `SMAppService.mainApp.status`를 실제 상태의 단일 원본으로 사용하고 별도 JSON 설정을 저장하지 않는지 확인
- 가짜 시스템 서비스로 미등록·활성·사용자 승인 필요·앱을 찾을 수 없음 상태와 등록·해제 실패 안내를 확인
- 일반 설정의 자동 실행 토글이 표시되는 대상 XCUITest 1개 통과
- 전체 단위 XCTest 303개 통과, 선택적 로컬 WebP fixture 1개 건너뜀, Debug 빌드 성공
- 실제 Xcode 실행에서 시스템이 현재 앱 경로의 등록 기록 없음으로 `.notFound`를 반환하는 것을 확인하고, 해당 상태에서도 첫 `register()`를 허용하도록 보완
- 보완 후 로그인 실행 대상 테스트 6개와 전체 단위 XCTest 307개 통과, 선택적 로컬 WebP fixture 1개 건너뜀
- 실제 앱의 켜기·끄기, 승인 필요 안내, 시스템 로그인 항목 반영과 재실행 복원은 수동 QA 대기

### 자동 규칙 앱 선택기 검증

- 측정일: 2026-07-24
- 실행 앱 후보에서 background accessory, MonglePet 자체, 중복·공백·누락 Identifier를 제외하고 표시 이름으로 정렬하는지 확인
- 임시 `.app` fixture에서 이름과 Bundle Identifier 추출, 일반 파일·Identifier 없는 앱·MonglePet 자체 선택 거부 확인
- 실행 중 앱, 앱 파일과 직접 입력 경로를 묶은 단일 대상 앱 선택 메뉴가 자동 규칙 탭에 표시되는 대상 XCUITest를 추가
- 전체 단위 XCTest 307개 통과, 선택적 로컬 WebP fixture 1개 건너뜀, Debug 빌드 성공
- 실제 실행 중 앱 선택, 앱 파일 선택과 규칙 저장 동작 수동 QA 통과
- 대상 앱 요약·단일 선택 메뉴·팝오버 직접 입력, 우선순위·입력 없음 시간 좌우 증감과 일반 탭 앱 관련 섹션 순서 개선
- 사용자 화면의 `유휴 규칙`을 `입력 없음 규칙`으로 바꾸고 등록된 규칙을 요약 카드와 펼침식 세부 편집으로 정리
- 등록 규칙의 제목 영역 전체 펼침·접힘, 아래·위 방향 표시와 배경색 없는 카드 내부 여백·전체 너비를 보완
- 개선 UI 접근성 항목을 대상 XCUITest에 추가하고 테스트 타깃 컴파일, Debug 빌드와 전체 단위 XCTest 307개 통과·선택 fixture 1개 건너뜀 확인
- 통합 메뉴의 세 선택 경로, 직접 입력 팝오버의 취소·적용, 접이식 규칙 카드의 오버플로우와 좌우 증감·숫자 입력은 실제 앱 수동 QA 대기

### 스프라이트 시트 애니메이션 가져오기 검증

- 측정일: 2026-07-27
- 정적 PNG·WebP 형식 확인, 확장자 불일치 거부, 균등 격자의 읽기 순서와 투명 배경 자동 경계 제안을 확인
- 단색 배경 제거가 기본적으로 원본을 유지하고 선택한 경우에만 가져오기 이미지에 알파를 적용하는지 확인
- 위·아래가 비대칭인 이미지에서 자동 경계는 좌측 상단 기준 읽기 순서를 유지하고, 배경 제거 전후 미리보기와 추출 프레임이 뒤집히지 않는지 확인
- 메모리에서 분리된 프레임이 기존 사용자 펫 공통 캔버스와 PNG atlas 작성 경계로 저장되는지 확인
- 전체 단위 XCTest 312개 통과, 선택 수동 fixture 2개 건너뜀, Debug 빌드 성공
- 내장 정적 WebP fixture로 스프라이트 분석기 대상 테스트 5개를 모두 다시 통과
- 방향 오류 보완 후 비대칭 상·하단 회귀 테스트를 포함한 스프라이트 분석기 대상 테스트 6개 통과
- 실제 1536×1872 WebP에서 자동 경계, 배경 제거 전환 시 방향 유지, 스크롤, 프레임 저장과 후속 생성·재생 수동 QA 통과

### 호버 쓰다듬기 검증

- 측정일: 2026-07-29
- 실제 표시 픽셀 진입 직후에는 실행하지 않고 300ms 연속 호버 뒤 한 번만 실행하는지 확인
- 같은 호버 중에는 반복하지 않고 패널 밖으로 이탈한 뒤 다시 진입해야 재실행하는지 확인
- 클릭 통과·동작 줄이기와 무관하게 쓰다듬기가 유지되고 미지정 시 불필요한 timer가 중지되는지 확인
- 자동 이동 중 포인터가 직접 표시 픽셀에 진입하면 실행하고, 펫이 정지한 포인터 아래로 이동한 경우와 사용자 드래그 중에는 오입력이 억제되는지 확인
- 기존 알파 영역 겹침 투명화와 기본 투명도 복원의 회귀 테스트를 함께 실행
- 포인터 생명주기 대상 단위 테스트 9개와 클릭 제거·드래그 경계 테스트 2개 통과
- 이동 중 허용 보완 후 전체 단위 XCTest 321개 통과, 선택적 로컬 WebP fixture 1개 건너뜀, Debug 빌드 성공
- 실제 앱에서 클릭 통과 켜짐·꺼짐 각각의 호버 반응, 이동 중 포인터 주도 쓰다듬기와 정지 포인터 아래로 이동할 때의 오입력 여부는 수동 QA 대기

### Phase 9C 방향별 이동 애니메이션 자동 검증

- 측정일: 2026-07-29
- 실제 적용 패널 좌표 변화량을 사용한 4방향·8방향 분류와 8도 경계 히스테리시스를 확인
- 정확한 방향, 같은 쪽의 가장 가까운 사용 방향, 모드 공통 fallback 순서와 같은 모션의 재시작 방지 구조를 확인
- 일부 방향만 지정했을 때 보조 축·대각선 자동 선택, 반대 방향과 5% 이하 미세 축 제외, 대각선 기능이 꺼진 참조 무시를 확인
- 마우스 따라가기·자유 이동의 독립 방향 설정, schema-v5 저장 왕복과 schema-v4 단일 모션 보존 마이그레이션을 확인
- 잘못된 개별 방향 ID 복구, 애니메이션 이름 변경·삭제 시 두 모드의 fallback·8방향 참조 갱신을 확인
- 권장 프로필 schema-v2 방향 설정 왕복, schema-v1 호환, `.monglepet` 내보내기·가져오기와 공유 전체 필드 요약을 확인
- 자동 선택 관련 이동 테스트 26개 통과
- 전체 단위 XCTest 337개 통과, 선택적 로컬 WebP fixture 1개 건너뜀, Debug 테스트 빌드 성공
- Codex 작업 세션의 기존 UI 자동화 충돌 기록에 따라 XCUITest는 실행하지 않았으며 실제 4·8방향 전환, 설정 저장 복원과 두 공유 검토 화면의 수동 QA 통과

### Phase 9D 마우스 도망가기 자동 검증

- 측정일: 2026-07-30
- 포인터와 펫 표시 사각형 사이 최단 거리, 반대 방향 도망 목표, 안전 거리 확보와 이동 범위 경계의 가장 먼 후보 대체를 확인
- 감지·해제 히스테리시스, 평상시 정지·자유 이동, 도망 속도와 도망 방향 애니메이션 전환을 확인
- settings schema-v5→v6 기본값 마이그레이션과 schema-v6 전체 필드 저장 왕복을 확인
- 권장 프로필 schema-v3 전체 필드 왕복, schema-v1·v2 호환과 공유 요약 전체 필드 검사를 확인
- 도망가기 모드의 쓰다듬기 UI 비활성화와 런타임 실행 차단, 클릭 통과·겹침 투명화의 독립 유지 구조를 확인
- 전체 단위 XCTest 346개 통과, 선택적 로컬 WebP fixture 1개 건너뜀, Debug 빌드 성공
- 실제 앱에서 평상시 정지·자유 이동 전환, 마우스 접근 회피, 클릭 통과·쓰다듬기 차단과 설정 복원 수동 QA 통과

### UI 자동화 환경 기록

- 설정창 UI 스모크 테스트는 Phase 1A에서 통과했다.
- Phase 1D 전체 UI 테스트 재실행은 Xcode Runner가 앱과 연결되기 전에 signal kill로 종료되어 완료하지 못했다.
- Phase 5B 설정창 UI 테스트는 test runner가 macOS automation mode를 활성화하는 단계에서 75초 후 timeout되어 완료하지 못했다.
- Phase 6 설정창 대상 UI 테스트에서 펫 미리보기와 애니메이션 목록의 접근성 노출을 확인했다.
- Phase 7 기존 XCUITest 5개는 모두 통과했지만 종료 직후 활성 Codex 앱의 `XCTAutomationSession`에서 `EXC_BAD_ACCESS`가 발생했다. Codex 작업 세션에서는 전체 UI 테스트를 실행하지 않고 Xcode에서 Codex가 비활성인 상태로 직접 실행한다.
- Phase 9-6 이동 탭 XCUITest는 추가하고 컴파일까지 확인했으며, 위 충돌을 피하기 위해 Codex 작업 세션에서는 실행하지 않았다.
- 설정창, 펫 오버레이, 메뉴 막대, 드래그, 표시 상태 전환과 화면 구성 변경은 실제 앱 수동 QA로 확인했다.

### 펫별 말풍선 자동 검증

- 측정일: 2026-07-30
- settings schema-v6→v7 비활성·빈 기본값 마이그레이션, v7 전체 필드 왕복과 잘못된 행동 루틴 대사 참조의 항목 단위 복구를 확인
- 주기 일회성 timer 재예약, 행동 루틴 진입 중복 억제, 비활성 상태와 재우기·잠금·절전 생명주기를 확인
- 말풍선의 화면 위·아래 배치, 수평 보정과 음수 좌표 디스플레이 계산을 순수 좌표 테스트로 확인
- 권장 프로필 schema-v4 전체 필드 왕복, schema-v1·v2·v3 기본값 호환과 공유 요약을 확인
- 관련 저장·패키지·런타임·배치 대상 테스트 통과, Debug 빌드 성공
- 실제 앱에서 주기·행동 대사, 펫 이동 추적, 클릭 통과와 재우기·잠금 중 숨김은 수동 QA 대기

### 말풍선 테마 자동 검증

- 측정일: 2026-07-31
- settings schema-v7→v8 호환 테마 마이그레이션, schema-v8 preset·사용자 색상·레이아웃·꼬리 전체 필드 왕복을 확인
- 사용자 지정 색상의 4.5:1 대비 제한과 읽기 어려운 저장값의 검정·흰색 글자 복구를 확인
- 말풍선 런타임 전달과 화면 위·아래 배치에 따른 꼬리 방향 계산을 확인
- 권장 프로필 schema-v5 테마 왕복과 schema-v4의 시스템 색상·꼬리 없음 호환 기본값을 확인
- 저장·공유·런타임·배치 관련 대상 XCTest 40개 통과, Debug 빌드 성공
- 실제 앱에서 preset 전환, 사용자 지정 색상, 레이아웃 slider, 꼬리 위치, 재실행 복원과 밝은·어두운 화면의 긴 대사는 수동 QA 대기

### 행동 우선·주기 대사 분리 자동 검증

- 측정일: 2026-07-31
- settings schema-v8→v9에서 기존 대사를 시간 지정으로 보존하고 주기 조건 유무에 따라 독립 사용 여부를 복구하는지 확인
- schema-v9의 주기 순서·행동 전환 정책·대사별 표시 방식 전체 왕복과 알 수 없는 enum의 항목 단위 복구를 확인
- 권장 프로필 schema-v6 전체 필드 왕복과 schema-v5의 무작위·닫기·시간 지정 호환 기본값을 확인
- 행동 대사의 주기 대사 즉시 선점, 연결 대사가 없는 행동 전환의 닫기·유지, 유지된 행동 대사의 주기 교체와 순차 재생 순환을 확인
- 시간 지정 대사는 표시 종료 후 전체 주기를 다시 기다리고, 재우기·잠금·절전에서 표시 종료와 주기 timer가 모두 취소되는지 확인
- 전체 단위 XCTest 378개 통과, 선택적 로컬 WebP fixture 1개 건너뜀과 Debug 빌드 성공
- 실제 앱에서 분리된 추가·수정·삭제, 순차·무작위 재생, 행동 전환과 재실행 복원은 수동 QA 대기

### 말풍선 상대 배치·다중 화면 도망·즉시 행동 전환 자동 검증

- 측정일: 2026-07-31
- settings schema-v9→v10 상대 배치 기본값 마이그레이션, schema-v10 전체 필드 왕복과 잘못된 위치·오프셋·간격의 독립 복구를 확인
- 권장 프로필 schema-v7 상대 배치 왕복과 schema-v6 자동·0pt·8pt 호환 기본값, 가져오기·내보내기 공통 요약을 확인
- 말풍선 자동·위·아래 선호, 좌우 오프셋·간격, 화면 경계와 음수 좌표 화면에서의 꼬리 기준점 보정을 확인
- 정지형 마우스 도망가기의 인접 화면 진입과 선택 화면 범위 우선, 실제 컨트롤러 경로·이동 활동 보고를 확인
- 다른 행동과 같은 ID의 편집 행동은 즉시 교체하고, 같은 행동 값은 진행 위치를 유지하며 일회성 상호작용 중 최신 일반 행동을 보존하는지 확인
- 전체 단위 XCTest 386개 통과, 선택적 로컬 WebP fixture 1개 건너뜀과 Debug 빌드 성공
- 실제 앱에서 현재 펫 첫 프레임을 포함한 말풍선 미리보기, 극단 좌우 위치의 경계 보정, 위치 편집·재실행 복원, 두 모니터 사이 정지형 도망과 실행 중 행동·애니메이션 편집은 수동 QA 대기

### 상태 메뉴 빠른 제어 자동 검증

- 측정일: 2026-07-31
- 현재 펫 이름, 일반화한 깨우기·재우기 문구, 클릭 통과 체크, 현재 화면으로 가져오기와 기존 설정·종료 메뉴 순서를 확인
- 상태 메뉴 callback과 외부 설정 변경에 따른 펫 이름·클릭 통과·깨움 상태 갱신을 확인
- 현재 화면으로 이동할 때 펫 크기를 유지하고 음수 좌표 디스플레이의 기본 우하단 위치를 사용하는지 확인
- 전체 단위 XCTest 387개 통과, 선택적 로컬 WebP fixture 1개 건너뜀과 Debug 빌드 성공
- 실제 앱에서 설정 화면과 클릭 통과 체크 동기화, 두 모니터 사이 현재 화면 가져오기와 위치 복원은 수동 QA 대기

### 입력 없음 규칙 즉시 해제 자동 검증

- 측정일: 2026-07-31
- 입력 없음 규칙 활성화 뒤 입력 시간이 0으로 바뀐 첫 snapshot에서 다음 자동 규칙 또는 기본 행동을 즉시 선택하는지 확인
- 입력 없음 행동의 애니메이션 사이클 도중 입력이 재개되어도 최신 행동의 첫 단계·첫 프레임으로 즉시 교체하는지 확인
- Resolver·Runtime 대상 XCTest 16개와 전체 단위 XCTest 388개 통과, 선택적 로컬 WebP fixture 1개 건너뜀과 Debug 빌드 성공
- 실제 앱에서 입력 없음 규칙 진입 후 사용자 입력에 따른 최대 약 1초 이내 전환은 수동 QA 대기

### 플랫폼 디렉터리 분리 검증

- 측정일: 2026-07-31
- macOS Xcode 프로젝트·소스·테스트를 `apps/macos/`로 함께 이동한 뒤 상대 그룹과 Swift Package 의존성 해석을 확인
- `apps/macos/MonglePet.xcodeproj` 기준 전체 단위 XCTest 388개 통과, 선택적 로컬 WebP fixture 1개 건너뜀과 Debug 빌드 성공
- Preview ZIP·Developer ID DMG 스크립트가 저장소 루트와 macOS 프로젝트 루트를 각각 계산하도록 수정하고 zsh 문법 검사 통과
- 루트 문서·배포 문서·샘플 경로와 작업 계획 인덱스의 새 경로 존재 확인

### Windows 로컬 공유 대상 자동 검증

- 측정일: 2026-08-09
- 권장 프로필 schema-v1 이동 모션 승격, schema-v4 말풍선 기본값 승격, schema-v7 왕복·앱 규칙 선택 제외와 누락 모션·1 MiB 제한을 확인했다.
- 가져오기 검토의 유효·잘못된·과대 권장 프로필 격리, 검토 후 원본 변경 거부와 일반 설치의 editor marker 제거를 확인했다.
- 내보내기의 canonical 파일 선별, compatibility 갱신, loader·ZIP 왕복과 재가져오기 검토를 확인했다.
- Settings 대상 테스트 50개, PetLibrary 대상 테스트 14개와 x64 Debug 앱 빌드가 경고·오류 없이 통과했다.
- Debug·Release 전체 153개 테스트와 Release 전체 빌드·MSIX 생성을 통과했다. 실제 파일 열기·저장 picker와 macOS 교차 왕복은 수동 QA에서 수행한다.

### Windows 로그인 자동 실행 구현 검증

- 측정일: 2026-08-09
- 기본 비활성 `desktop:StartupTask` 선언이 생성 AppxManifest에 유지되고 StartupTask 실제 상태를 일반 설정 UI에 매핑하는지 확인했다.
- x64 Debug·Release 앱 빌드, 두 구성 전체 153개 테스트와 startup task가 포함된 Release MSIX 생성이 통과했다.
- extension 실행 파일에 빌드 토큰을 사용하면 MakeAppx가 치환하지 않아 거부하는 문제를 `MonglePet.Windows.exe` 명시로 수정했다.
- 실제 시작 앱 등록·해제와 다음 로그인 실행은 최종 설치 QA에서 확인한다.

### Windows unpackaged EXE 설치기 검증

- 측정일: 2026-08-09
- package identity 분기, 인용된 현재 실행 파일 Run 명령과 기존 MSIX LocalState의 비파괴 staging 복사를 Shell 단위 테스트로 확인했다. 대상 데이터가 있으면 덮어쓰지 않고 원본이 없으면 대상도 만들지 않는다.
- x64 self-contained publish는 AppxManifest 없이 538개 파일 약 225.05MiB를 생성했고 실행 파일 FileVersion `1.0.0.13`, Windows App Runtime 포함과 미서명 상태를 확인했다.
- Inno Setup 6.7.3으로 약 60.8MiB의 `MonglePet-Windows-1.0.0.13-x64-Setup.exe`와 일치하는 SHA256SUMS를 생성했다.
- 실제 사용자별 최초 설치에서 540개 파일, 시작 메뉴와 단일 제거 항목을 확인했고 자동 실행과 데이터 폴더는 기본으로 만들지 않았다. 같은 AppId의 동일 버전 설치기로 업그레이드했을 때 설치 EXE와 publish EXE의 SHA-256이 일치했다.
- `--startup` 실행의 모든 최상위 HWND를 열거해 펫 오버레이만 보이고 `WinUIDesktopWin32WindowClass` 설정창은 숨김인 것을 확인했다. 기존 MSIX 데이터 11개는 `%LOCALAPPDATA%\MonglePet`에 복사되며 원본 11개가 유지됐다.
- 실행 중 제거는 숨은 notification area HWND의 전용 종료 메시지로 앱의 정상 종료 루틴을 호출했다. 프로세스·설치 폴더·시작 메뉴·제거 항목·정확히 일치하는 Run 값이 모두 없어지고 사용자 `settings.json` SHA-256은 유지됐다.
- 설치기와 앱은 미서명이므로 SmartScreen·Smart App Control·조직 정책의 경고 또는 차단 가능성이 남는다. 공개 전 공식 웹 다운로드, GitHub Release 사본과 SHA-256 일치, 깨끗한 사용자 계정에서의 대화형 UI를 다시 확인한다.

### macOS 멀티펫 1단계 버전·schema-v11 계약 검증

- 측정일: 2026-08-13
- macOS 앱 마케팅 버전 `1.1.0`, 빌드 번호 `2`와 설정 화면 표시값 `MonglePet 1.1.0 (2)`의 자동 테스트를 통과했다.
- 새로 내보내는 `.monglepet`의 제작 앱 버전과 최소 필요 앱 버전이 모두 `1.1.0`으로 기록되는지 확인했다.
- schema-v10 선택 펫을 첫 활성 인스턴스로 이관하고, 선택되지 않은 프로필 보존·선택 프로필 연결·누락 기본 프로필 생성·UUID 안정 왕복을 검증하는 v11 마이그레이션 테스트 4개를 통과했다.
- 버전·v11 마이그레이션·패키지 내보내기 대상 XCTest 22개가 통과했다. 설정 화면 버전과 기존 패키지 가져오기 수동 QA는 멀티펫 런타임 연결 뒤 수행한다.
- macOS 전체 `MonglePetTests` 회귀가 통과했다.
- `shared/Fixtures/Settings`의 schema-v10 입력과 고정 UUID schema-v11 기대 결과를 macOS codec으로 비교하는 공통 계약 테스트를 추가했다. 해당 테스트를 포함한 `AppSettingsV11MigrationTests` 5개가 통과했다.

### macOS 멀티펫 2단계 Domain·저장소 검증

- 측정일: 2026-08-13
- schema-v11 유효 문서의 식별자 무변경 왕복, 같은 원본 펫 두 인스턴스의 독립 프로필·overlay 저장, 중복 instance/profile UUID와 공유 참조·별명·표시 상태·overlay·display order의 항목별 복구를 검증했다.
- schema-v10 설정을 처음 읽을 때 v11로 원자적으로 교체하고, 두 번째 로드에서 생성된 instance/profile UUID가 바뀌거나 파일을 다시 마이그레이션하지 않는지 확인했다.
- 현재 단일 런타임 설정 편집이 선택 인스턴스만 바꾸고 저장·재실행 뒤 다른 인스턴스 데이터를 보존하도록 Store·Session 회귀를 통과했다.
- Debug 빌드 성공, 전체 `MonglePetTests` 398개 통과, 선택형 로컬 WebP fixture 1개 건너뜀을 확인했다.

### macOS 멀티펫 3단계 런타임 소유권 분리 검증

- 측정일: 2026-08-13
- `PetInstanceManager`가 선택 instance ID별 context를 생성·재사용하고 선택 변경에서 이전 단일 context를 정리하며, 잘못된 선택 참조에는 창을 만들지 않는지 확인했다.
- 프로세스 공통 activity snapshot과 reduce motion 상태를 기존 context와 새 context에 전달하고, Coordinator의 기존 잠금·절전·접근성 이동 제한 동작을 유지하는지 검증했다.
- 런타임 좌표 변경이 전달한 instance ID의 overlay만 저장하고 선택된 다른 인스턴스와 행동 프로필을 바꾸지 않는지 저장·재로드로 확인했다.
- Debug 빌드 성공, 전체 `MonglePetTests` 404개 통과, 선택형 로컬 WebP fixture 1개 건너뜀을 확인했다. 다중 overlay를 실제로 동시에 만드는 동작은 4단계 검증 범위다.

### macOS 멀티펫 8단계 자원 제어·안전 시작 검증

- 측정일: 2026-08-13
- 단계적 복원 허용 목록이 기존 런타임을 다시 적용하거나 중단하지 않고 새 인스턴스만 순서대로 추가하는지 확인했다.
- 실행 전용 `모두 일시정지`가 기존 런타임과 일시정지 중 새로 복원한 런타임 모두에 전달되고 해제 시 함께 재개되는지 확인했다.
- 두 마리 이상에서 CPU·resident memory 압력이 두 번 연속 기준을 넘을 때만 경고하고 한 마리 또는 정상 상태에서는 경고하지 않는 순수 evaluator 테스트를 통과했다.
- 시작 복구 저널의 정상·안전 모드 왕복, 손상·4KiB 초과 파일 제거, 복원 실패 시 현재 인스턴스 marker 유지와 성공 시 단계별 제거를 검증했다.
- 실제 `AppCoordinator` 연결에서 미완료 복원 marker가 있으면 펫을 자동 표시하지 않고, 사용자 전체 복원 후 런타임과 복구 상태가 정상화되는지 확인했다.
- Debug 빌드와 전체 `MonglePetTests` 434개 통과, 선택형 로컬 WebP fixture 1개 건너뜀을 확인했다.
- 활성 펫 XCUITest는 새 전체 일시정지 버튼까지 컴파일됐으나 현재 자동 실행 환경에서 Runner가 앱 연결 전에 종료돼 실행 결과는 Xcode 수동 QA에 남겼다.

### macOS 멀티펫 9단계 Release 자동 기준선

- 측정일: 2026-08-13
- 환경: Apple M2 Pro 12코어·16GB, macOS 26.6(25G72), unsigned arm64 Release 빌드
- `measure-multi-pet-release.zsh`가 `--ui-testing` 임시 설정에 내장 펫 1~64마리를 만들고 지정 시간이 지나면 정상 종료하도록 구성했다. 일반 실행에서는 QA 인자를 해석하지 않으며 사용자 설정과 펫 보관함을 사용하거나 변경하지 않는다.
- `ps`의 프로세스 `%CPU`와 RSS를 1초 간격으로 기록했다. 위치 고정 1·2·4·8마리 10초 측정의 평균 CPU는 각각 0.280%·0.250%·0.290%·0.350%, 최대 CPU는 0.700%·0.400%·0.500%·0.900%였고 RSS는 약 60.1~63.2MiB였다.
- 마우스 따라가기 1·4·8마리 10초 측정은 평균 CPU 2.190%·3.460%·4.650%, 최대 CPU 5.500%·8.500%·11.000%였다. 자유 이동은 평균 CPU 4.390%·3.550%·8.330%, 최대 CPU 5.400%·9.200%·10.200%였다. 모든 RSS는 약 61.4~64.9MiB 범위였다.
- 8마리 30초 재측정에서 마우스 따라가기는 평균 CPU 7.763%·최대 11.100%·최대 RSS 64.6MiB, 자유 이동은 평균 CPU 4.363%·최대 10.700%·최대 RSS 60.7MiB였다. 순간 10% 초과는 있었지만 앱의 30% 지속 경고 기준에 도달하지 않았고 측정 중 메모리 증가도 관찰되지 않았다.
- 전체 `MonglePetTests`는 437개 통과, 선택형 로컬 WebP fixture 1개 건너뜀, 실패 0개이며 Release 빌드도 통과했다.
- 실제 앱에서 기존 1마리 재우기·깨우기·이동·행동·말풍선·재실행 복원, 같은 펫 3마리 이상의 위치·이동·행동 독립성, 두 모니터 사이 세 자동 이동 모드, 화면 분리·재연결 복귀, 전체 일시정지·재개와 영속 앞뒤 순서를 사용자 확인했다.
- 8마리 마우스 따라가기 Release workload를 일반 작업과 함께 약 5분 실행해 시각적 끊김과 기능 이상이 없음을 확인한 뒤 정상 중단했다. 중단된 TSV에는 완결된 측정값이 없으므로 결과로 사용하지 않았다.
- 1시간 메모리 증가와 Instruments GPU·프레임 지연은 사용자 판단에 따라 macOS 9단계 필수 조건에서 제외하고 공개 배포 전 안정화 QA로 이관했다.

### Windows 멀티펫 10단계 schema-v11 portable 계약 검증

- 측정일: 2026-08-13
- Windows 마케팅 버전 `1.1.0`, MSIX·파일 버전 `1.1.0.13`과 새 `.monglepet`의 제작 앱·최소 필요 앱 버전 `1.1.0`을 적용했다.
- schema-v11·마이그레이션 대상 Settings 테스트 13개와 Settings 전체 63개가 통과했다. 공통 `schema-v10-single-pet.json`이 고정 UUID의 `schema-v11-single-instance.expected.json`과 정확히 같은 결과로 이관됨을 확인했다.
- 같은 설치 펫 두 인스턴스가 별도 instance/profile ID와 profile·overlay를 저장하고 재로드한 뒤에도 독립적이며, 선택 인스턴스 호환 view로 수정해도 다른 인스턴스가 바뀌지 않음을 확인했다.
- 중복·잘못된 instance/profile UUID, 누락·공유 프로필, 펫 키 불일치, 잘못된 선택 참조·별명·display order를 항목별로 복구하고 살아남은 인스턴스·프로필의 알 수 없는 확장 필드를 저장 뒤에도 보존하는지 확인했다.
- x64 Debug·Release 전체 솔루션 빌드가 오류 없이 통과했고 Activity 27개, Core 38개, Packages 18개, PetLibrary 18개, Settings 63개, Shell 12개로 각 구성 176개 테스트가 모두 통과했다. NuGet 취약성 피드 조회 실패에 따른 `NU1900` 경고 2건은 두 빌드에 남았다.
- 이 검증 시점에는 11단계 범위인 다중 Win32 HWND, `PetInstanceManager`, NavigationView 활성 펫 UI, notification area 멀티펫 메뉴, 자원 경고와 안전 시작 UI를 구현하지 않았으며 아래 11단계에서 후속 완료했다.

### Windows 멀티펫 11단계 runtime·관리 UX 검증

- 측정일: 2026-08-13
- `ActivePetSettingsEditor`가 같은 원본의 독립 profile·offset overlay 추가, instance별 편집 격리, display order 재정렬, 최소 한 마리 보호 제거와 설치 삭제 시 모든 참조의 독립 내장 프로필 전환을 검증했다.
- notification area의 전체 명령 우선 순서, instance별 선택·표시·클릭 통과·현재 화면 이동 식별자, 안전 시작·자원 경고 표시를 검증했다.
- 4KiB 제한 원자적 복원 저널의 기록·완료·손상/과대 입력 안전 시작과 다수 펫에서만 연속 CPU·private memory 압력을 경고하는 evaluator를 검증했다.
- x64 Debug·Release 전체 솔루션 빌드가 통과했고 Activity 27개, Core 38개, Packages 18개, PetLibrary 18개, Settings 69개, Shell 16개로 각 구성 186개 테스트가 모두 통과했다. NuGet 취약성 피드 조회 실패 `NU1900` 경고 2건은 남았다.
- 실제 다중 HWND 시각·상호작용, 혼합 DPI·음수 좌표 모니터, 물리 잠금·절전 복귀, Explorer 재시작, packaged Release 자원과 1.0.x→1.1.0 설치 업데이트는 Windows 12단계 수동 QA로 남겼다.
- 2026-08-13 부분 실기기 QA에서는 build 26200의 100% DPI 1920×1080 화면 2대에서 1.0.0.13→1.1.0.13 등록 업데이트와 v10→v11/라이브러리 SHA-256 보존, 3개 독립 instance/profile/overlay HWND, 개별·전체 표시, 일시정지, 재실행, native notification area 하위 메뉴, Explorer 재시작, journal 안전 시작/복원을 통과했다. 3마리 Release 30초 측정은 평균 CPU 11.81%, private memory 최대 115.6MiB, working set 최대 186.0MiB, GPU engine peak 1.47%였다.
- 화면 경계 보정 저장 누락을 QA 중 수정했다. owned topmost 말풍선이 펫 그룹 순서를 바꾸던 문제는 말풍선을 비활성 독립 tool window로 분리하고 `말풍선→펫` 그룹을 명시적 HWND 체인으로 정렬해 해결했다. 말풍선 2개가 동시에 보이는 15초 12개 샘플, display order 변경과 재실행에서 모든 그룹 rank와 말풍선-펫 내부 순서가 일치했다. 혼합 DPI, 모니터 분리·재연결과 실제 잠금·절전은 환경 또는 수동 조작이 필요해 남아 있다.
- 2026-08-14 이동 cadence와 unchanged activity 전파 수정 뒤 마우스 따라가기 1마리·자유 이동 1마리를 동시에 실행한 Release 5분 60표본은 전체 시스템 CPU 평균 1.484%·최대 3.594%, 단일 코어 환산 평균 8.90%·최대 21.56%였다. private memory는 127.73→133.29MiB(+5.57MiB), 최대 134.68MiB였고 120초 이후 134.20→133.29MiB로 안정됐다. working set은 190.33→192.96MiB, 최대 197.04MiB였으며 무응답과 같은 시간대 관련 Application 오류는 0건이었다. D-079의 5분 기준을 통과했다.
- 활성 펫 목록은 상태 이벤트 중 collection 전체 교체를 하지 않고 instance ID별 view model을 제자리 갱신한다. 실제 Release UI에서 별칭 TextBox에 포커스를 둔 뒤 500ms 간격 8회 동안 동일 Automation Runtime ID와 포커스가 유지되어 반복 등장 전환과 편집 포커스 초기화가 재현되지 않았다.
- 2026-08-16 Windows `1.1.0.13` unpackaged Preview 후보에서 `--startup`이 첫 WinUI 활성화 전에 `AppWindow.Hide()`를 호출해 `E_NOINTERFACE`로 종료되는 문제를 재현했다. 주 창을 먼저 활성화하고 초기화 뒤 Win32 `ShowWindow(SW_HIDE)`로 HWND만 숨기도록 수정했다. 게시본과 실제 사용자별 업그레이드 설치본의 일반 실행·`--startup`이 각각 응답 상태를 유지하고 전용 메시지로 정상 종료됐으며 관련 Application 오류 0건, 라이브러리 10개 파일 해시 보존을 확인했다. x64 Debug·Release 빌드와 각 구성 186개 테스트도 통과했다.

### Windows 1.2.0 Preview 배포 사전 검증

- 측정일: 2026-08-24
- Windows 마케팅 버전 `1.2.0`, Assembly·File·MSIX 버전 `1.2.0.13`의 자동 계약 테스트를 추가했다.
- x64 Debug·Release 전체 솔루션 빌드와 Activity 27개, Core 38개, Packages 18개, PetLibrary 63개, Settings 69개, Shell 20개로 각 구성 235개 테스트가 모두 통과했다.
- 기존 `1.1.0.13` 개발 MSIX를 `1.2.0.13`으로 등록 업데이트해 LocalState 22개 파일의 크기·SHA-256 차이 0개를 확인했다. 실제 packaged 개발 URL이 검토 화면을 열었고 취소 뒤 라이브러리 차이와 임시 폴더가 0개였다.
- 초기 EXE 설치기 후보를 기존 `1.1.0.13` 위에 설치했을 때 `%LOCALAPPDATA%\MonglePet` 11개 파일의 크기·SHA-256 차이가 0개였고 설치된 파일 버전과 protocol command가 `1.2.0.13` 실행 파일을 가리켰다.
- EXE의 정확한 protocol 명령으로 종료 상태와 실행 중 상태 모두 실제 개발 URL 검토·취소를 통과했고 라이브러리 변경과 임시 폴더가 0개였다. 같은 PC에 개발 MSIX와 EXE가 함께 등록된 상태에서는 Windows Shell scheme 선택이 실행되지 않아 두 배포 채널 공존 association QA는 별도 환경 후속으로 남겼다.
- 초기 후보에서 전용 종료 뒤 창·트레이는 닫히지만 프로세스가 남는 결함을 재현했다. 마지막 창을 닫기 전에 낮은 우선순위로 `Application.Exit`를 예약하도록 수정한 Release publish는 검토·취소 뒤 전용 종료 메시지만으로 프로세스 0개가 됐다.

### macOS 1.2.0 Preview 배포 사전 검증

- 측정일: 2026-08-23
- macOS 앱 마케팅 버전 `1.2.0`, 빌드 번호 `3`과 설정 화면 표시값 `MonglePet 1.2.0 (3)`의 자동 테스트를 통과했다.
- 새로 내보내는 `.monglepet`의 제작 앱 버전과 최소 필요 앱 버전이 모두 `1.2.0`으로 기록되는지 확인했다.
- 전체 `MonglePetTests` 456개 중 455개가 통과했고 실패 0개였다. 외부 로컬 WebP fixture가 있을 때만 실행하는 기존 선택형 테스트 1개는 건너뛰었다.
- 코드서명 없는 Debug 테스트 빌드와 `generic/platform=macOS` Release 빌드가 통과했다. Release 앱의 `CFBundleShortVersionString=1.2.0`, `CFBundleVersion=3`, 실행 파일 `arm64`·`x86_64` Universal 구성을 확인했다.
- 깨끗한 원격 커밋 `8eb6e179f952aadce1623ac9abcefce89bde1044`에서 7,006,673 bytes의 `MonglePet-1.2.0-build.3-preview.zip`과 SHA-256·manifest를 생성했다. ZIP SHA-256 `4E6347A939D53ACDE931D90B0B89FD65943D2EECB2266A1C83BA7FD0AF67AE93`이 재계산 결과와 일치했다.
- 별도 임시 디렉터리의 압축 해제본에서 버전·Bundle ID·Universal 아키텍처·미서명 상태를 확인하고 `--ui-testing --ui-testing-open-settings`로 실제 프로세스가 정상 실행되는지 확인했다.
- GitHub 태그 `macos-v1.2.0-preview.1`의 Pre-release에 ZIP·SHA-256·manifest를 게시한 뒤 세 파일을 다시 내려받아 ZIP digest와 보조 파일이 로컬 최종본과 바이트 단위로 일치함을 확인했다.

## 변경 유형별 최소 검증

### 후속 단계 필수 검증

- 펫 스튜디오: 편집 실패 원자성, 기본·마지막 애니메이션 보호, 편집 사본의 원본 불변성
- 애니메이션·행동 v2: 프레임별 시간 경계, 사이클 반복 횟수, schema-v1 fixture 마이그레이션
- 펫별 행동: 내장·설치·별도 사본 프로필 격리, 같은 설치 교체 유지와 삭제 정리
- 로컬 공유: `.monglepet` 내보내기·가져오기 왕복, marker·설치 UUID·행동 설정 제외와 ZIP 보안 제한
- 펫 이동 모드: 결정적 이동 좌표, 드래그·잠금·절전 생명주기, 다중 화면과 비활성 상태 timer·CPU 기준선
- Phase 9B 표시 보완: 화면 연결 경로, 이동 범위, schema-v4 마이그레이션, 알파 겹침 투명도와 앱·패키지 SemVer 호환
- Phase 9C 방향 이동 표시: 실제 벡터 분류·히스테리시스, schema-v5와 권장 프로필 v1·v2, 이름 변경·삭제와 공유 전체 필드 왕복
- Phase 9D 마우스 도망가기: 사각형 거리·범위 경계 목표·해제 히스테리시스, schema-v6와 권장 프로필 v3, 쓰다듬기 차단·클릭 통과 독립성
- 펫별 말풍선: schema-v10·권장 프로필 v7, 행동 우선·주기/표시 timer, 전환 정책·테마 대비·상대 배치·공유 왕복, child panel 클릭 통과·화면 경계·연속 꼬리 외곽선과 숨김 생명주기

| 변경 유형 | 최소 검증 |
| --- | --- |
| 문서만 변경 | Markdown 링크와 경로 검사 |
| 순수 Domain 변경 | 관련 단위 테스트 |
| 저장·패키지 변경 | 단위 테스트와 임시 디렉터리 통합 테스트 |
| AppKit 창 또는 런타임 변경 | 빌드, 관련 단위 테스트, 수동 실행 |
| SwiftUI 설정 화면 변경 | 빌드와 관련 UI 스모크 테스트 |
| 릴리스 관련 변경 | 전체 테스트, 수동 QA, 성능 기준선 |

---

문서 상태: active
마지막 갱신: 2026-08-24
